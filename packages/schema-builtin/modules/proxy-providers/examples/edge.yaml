# Boundary-valid: the "file" branch only strictly needs "path" — every
# optional shared field (health-check/override/filter) is intentionally
# absent to prove the minimal-required-fields case is schema-valid.
type: file
path: ./local-providers/private.yaml
