node_modules
dist
coverage
pnpm-lock.yaml

# Source documents authored outside this repo: kept verbatim so a reformat never
# shows up as a content change when they are revised upstream.
DESIGN.md
mihomo-config-studio-prd.md

# Golden fixtures are byte-significant: formatting them would invalidate round-trip tests.
packages/test-fixtures/fixtures/**

# Capacitor-generated native project: not our source, regenerated by `cap sync`.
apps/android/android/**
