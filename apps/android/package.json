{
  "name": "@mcs/android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capacitor product shell loading apps/web's build output (FR-AND-01..06, ADR-001/ADR-026). webDir is ../web/dist — run `pnpm run build` (web) before `cap sync android`.",
  "scripts": {
    "build": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "7.6.8",
    "@capacitor/app": "7.1.2",
    "@capacitor/core": "7.6.8"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.8"
  }
}
