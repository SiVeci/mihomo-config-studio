{
  "name": "@mcs/egress-check",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Asserts packages/** never performs network I/O outside a fixed, path-exact allowlist, and that the allowlisted file never sends a request body (NFR-SEC-01, v0.5.0 decision F5).",
  "exports": {
    ".": "./src/index.ts"
  }
}
