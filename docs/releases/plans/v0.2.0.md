# v0.2.0 执行计划 —— 配置内核与 Web 骨架

需求来源：[v0.2.0 版本文档](../v0.2.0-config-core-and-web-shell.md)。本文件只做拆解与排序，
不新增、不删减需求。两者冲突时以版本文档为准。

## 基线

| 项目                     | 实测值                                                                                                                                                                                  |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 生成日期                 | 2026-08-13                                                                                                                                                                              |
| 基线 commit              | `d95dc9b`（docs: 标记切片 #14 为 Done 并记录提交 SHA，v0.1.0 全部切片收口），分支 `chore/scaffold-baseline`                                                                             |
| `main` 实际位置          | **`ee97ccc` Initial commit** —— v0.1.0 的 14 个切片一个都没合并进 main，见先决项判定第 3 条                                                                                             |
| 工作区状态               | 干净（`git status --short` 无输出）                                                                                                                                                     |
| `pnpm run check`         | **全绿**，exit 0（typecheck + lint + test）                                                                                                                                             |
| `pnpm run format:check`  | 全绿                                                                                                                                                                                    |
| 测试                     | 22 个文件 / **238 例**全通过                                                                                                                                                            |
| 覆盖率                   | 语句 **94.51%** / 分支 **90.82%** / 函数 **94.05%** / 行 **94.51%**                                                                                                                     |
| 现存包                   | `yaml-engine`、`schema-core`、`schema-registry`、`form-renderer`、`config-model`、`graph`、`test-fixtures`；`tools/{schema-cli,android-manifest-check,webcrypto-probe}`；`apps/android` |
| **本版本要从零新建的包** | `packages/storage`、`packages/project-format`、`packages/ui`、`packages/validator`、`apps/web` —— **五个目录全部不存在**                                                                |

依赖现状：仓库**没有** vite、react-router、i18n 库、zip 库、playwright 中的任何一个，
均需本版本按切片引入。React 19、react-dom、`@testing-library/react`、jsdom 已在根
`devDependencies`（`form-renderer` 测试在用），`.tsx` 的 lint 与 `jsx: react-jsx` 编译
路径已验证可用（`packages/form-renderer`）。jsdom 用例走 `// @vitest-environment jsdom`
文件头注释，不改根级 `environment: 'node'`。

golden 语料现状：`packages/test-fixtures/fixtures/yaml/` 最大一份是 `comprehensive.yaml`
**3847 字节**。NFR-PERF-02 要求的 1 MB 语料**不存在**，见 #16。

## 先决项判定

版本文档列两条。逐条核对**代码**而非文档声明，结论如下。

### 1. v0.1.0 的 `config-model` / `graph` 已通过 M0-3 验证 —— 已满足

不看追踪表的声明，看测试文件本身：`config-model/src/entity.test.ts`（12 例）、
`rule-line.test.ts`（14 例）、`graph/src/reference-index.test.ts`（15 例）、
`impact.test.ts`（9 例）、`cycles.test.ts`（9 例），共 59 例，在本次基线的 238 例中
全部通过。ADR-009 已 Accepted。**无需补片。**

### 2. ADR-011 的对比度修正方案已确认 —— 已满足，且数值经独立复算无误

按 WCAG 2.x 相对亮度公式独立复算 ADR-011 §2 的六个组合，**全部精确复现**：

| 组合                       | ADR-011 记载 | 本次复算 |
| -------------------------- | ------------ | -------- |
| `body` on `canvas`         | 10.34        | 10.34    |
| `muted` on `canvas`        | 5.13         | 5.13     |
| `muted-soft` on `canvas`   | 3.23         | 3.23     |
| `primary` on `canvas`      | 3.11         | 3.11     |
| 白字 on `primary`          | 3.28         | 3.28     |
| `on-dark-soft` on 暗面     | 6.62         | 6.62     |
| `primary-active` on canvas | 4.80         | 4.80     |
| 白字 on `primary-active`   | 5.06         | 5.06     |

先决项本身成立，**无需补片**。但复算同时暴露了 ADR-011 未覆盖、且会让退出条件
「CI 对比度断言通过」直接 fail 的两组组合，已定案为 **D1**（见文末「已确认的决策」），
落到 #7 / #8。

### 3. 版本文档未列、但会挡住第一条分支命令的前置事实 —— 需补片

`main` 是 `ee97ccc Initial commit`（空仓库）。v0.1.0 的全部成果只存在于
`chore/scaffold-baseline` 与 15 个 `feat/v0.1.0-*` 分支上。工程约束要求
「不在 main 上直接提交，按 `feat/v0.2.0-切片名` 建分支」——但从 main 切出来就是从
空仓库开始。已定案为 **D4**：先把 v0.1.0 合进 main。→ **切片 #0**。

## 现状核对：追踪表 / README 与代码不符的行

逐条以代码为准。追踪表的 FR/NFR **状态列本身与代码一致，无虚标**；不符集中在
统计数字与阶段描述：

| 位置                                                     | 现状                                                                                        | 应改为                                                                                                                                                        | 修在哪片 |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `requirements-traceability.md:139`                       | NFR-MAINT 行写「全局 92.14% 行覆盖」                                                        | 94.51%（92.14 是 v0.1.0 #5 时期旧数，与本文件表头自相矛盾）                                                                                                   | #0       |
| `requirements-traceability.md:12`                        | 分支 90.82%                                                                                 | **无需改动**——#0 执行时用 `pnpm run test:coverage` 复核两次，稳定得 90.82%，与本行原文一致；本计划基线表「90.83%」是笔误，已在本文件「基线」表同步改回 90.82% | #0       |
| `requirements-traceability.md:22`                        | 「M1 骨架与配置内核 \| 进行中」                                                             | M1 代码为零，本行在 #0 前不成立；改为「进行中（v0.2.0）」                                                                                                     | #0       |
| `README.md:32`                                           | 「早期开发中（M0 技术风险验证阶段）」                                                       | M0 已收口，改为 M1                                                                                                                                            | #0       |
| `README.md:57-62`                                        | tools 区列了不存在的 `upstream-watch`、`core-test-runner`，漏了实际存在的 `webcrypto-probe` | 未建的标「规划中」，补 `webcrypto-probe` 行                                                                                                                   | #0       |
| `docs/releases/README.md:14`                             | v0.2.0 状态「未开始」                                                                       | 进行中                                                                                                                                                        | #0       |
| `packages/schema-registry/src/store.ts:11-13`            | 注释「真实持久化实现属于 `@mcs/storage`（v0.2.0）」                                         | 本版本兑现：#4 提供 `StorageAdapter` 支撑的 `BundleStore`                                                                                                     | #4       |
| `packages/schema-registry/src/testing/memory-store.ts:3` | 同上                                                                                        | 同上                                                                                                                                                          | #4       |

## 进度表

| #   | 名称                                     | 依赖     | 状态 | 提交 SHA  |
| --- | ---------------------------------------- | -------- | ---- | --------- |
| 0   | v0.1.0 合入 main、文档校正与计划归档     | —        | Done | `2988e7b` |
| 1   | `validator` 骨架：统一 `ValidationIssue` | #0       | Done | `55c5600` |
| 2   | 校验流水线、阶段编排与阻断判定           | #1       | Done | `e20f5cf` |
| 3   | `config-model` 统一历史栈（撤销/重做）   | #0       | Done | `fd72557` |
| 4   | `storage` 端口、内存与 IndexedDB 适配    | #0       | Done | `aba60f7` |
| 5   | 自动保存、快照与空间裁剪策略             | #3、#4   | Done | `61ce038` |
| 6   | `project-format`：`.mcsproj` ZIP 往返    | #4       | Todo |           |
| 7   | `packages/ui` 设计令牌与文字色层         | #0       | Done | `5d36361` |
| 8   | 对比度断言与 `DESIGN.md` 迁移            | #7       | Done | `ee750cc` |
| 9   | `apps/web` 脚手架、路由与 i18n 资源结构  | #7       | Done | `096ec90` |
| 10  | Worker 边界：解析 / 校验 / 差异出主线程  | #2、#9   | Done | `9bb6196` |
| 11  | 三栏布局壳与项目管理页                   | #5、#9   | Done | `94d1228` |
| 12  | YAML 导入入口（文件 / 粘贴 / 剪贴板）    | #10、#11 | Done | `877d2cb` |
| 13  | 原文编辑器与非法时冻结结构化视图         | #12      | Done | `50f88d1` |
| 14  | 问题中心与点击跳转 YAML 行               | #13      | Done | `247c097` |
| 15  | 差异面板、导出与敏感数据提示             | #6、#14  | Todo |           |
| 16  | 1 MB 语料性能基准                        | #10      | Todo |           |
| 17  | 闭环验收与文档收口                       | 全部     | Todo |           |

三条链在 #0 之后可并行推进，链内严格按依赖顺序：

- **链 A 校验内核**：#1 → #2
- **链 B 数据层**：#3、#4 → #5；#4 → #6
- **链 C 设计系统**：#7 → #8

#9 起汇流成单链（#9 → #10 → #11 → #12 → #13 → #14 → #15），#16 只需 #10，
可与 #11–#15 并行。

## 工程约束（每片实现时都适用）

- Node ≥ 20.11 / pnpm 10。验收 = `pnpm run check` **和** `pnpm run format:check`。
  后者在 CI 是独立步骤，最容易漏。
- 覆盖率阈值（行 / 函数 / 语句 85、分支 80）在 `vitest.config.ts` 强制，**不得放宽**。
  #9 起 `apps/web/src/**` 一并纳入 coverage `include`，阈值不变（见 #9）。
- `packages/**` 禁止 `node:fs`（ESLint `no-restricted-imports`）与任何网络调用
  （CI `no-network-egress` job 用 grep 拦 `fetch|XMLHttpRequest|WebSocket|sendBeacon`）。
  需要平台能力的一律定义端口接口 + 由调用方注入。**注意：该 grep 作用于 `packages/**`，
  `apps/web` 不在其覆盖范围内——本版本 `apps/web` 也不得出现任何上传路径，靠 review 保证。**
- 工作区内部包直接导出 TypeScript 源码（ADR-007）：`package.json` 的 `exports` 指向
  `./src/index.ts`，不产出构建产物。`apps/web` 是消费方，由 Vite 打包。
- 新增包必须同时登记：`packages/<name>/package.json`、`tsconfig.json`、
  根 `tsconfig.build.json` 的 `references`，否则 `pnpm run typecheck` 不覆盖它。
- 用户可见文本一律 i18n key，不写字面量（ADR-016）。中英资源文件的 key 集合必须相等，
  由 #9 的断言测试强制。
- 错误与日志消息不得包含配置值（NFR-SEC-03）。`ValidationIssue` 只携带 i18n key +
  受限参数（路径、字段名、Schema 常量），**不携带用户输入的值**。
- `packages/test-fixtures/fixtures/**` 是字节敏感的 golden 文件：**只新增，不修改，
  不格式化**（已在 `.prettierignore` 与 `.gitattributes` 排除）。
- 新增架构决策写新 ADR（本版本预计占用 **ADR-017、018、019**），不改已 Accepted 的。
- PRD 与上游实际行为冲突时以上游为准，记入 `docs/upstream-divergences.md`，
  不得静默改需求。
- 分支名 `feat/v0.2.0-<切片名>`，从 **main** 切出（#0 之后 main 才有内容），
  提交信息用中文。

---

## 切片详情

### #0 v0.1.0 合入 main、文档校正与计划归档

**目标**：让 main 与追踪表的声明一致，并把本计划纳入版本控制，建立可追溯基线。

**覆盖**：无 FR/NFR（工程前置）。

**涉及文件**

- 新建：`docs/releases/plans/v0.2.0.md`（本文件）
- 修改：`docs/requirements-traceability.md`（第 12、22、139 行，见「现状核对」表）
- 修改：`README.md`（第 32 行阶段描述；第 57–62 行 tools 区）
- 修改：`docs/releases/README.md`（第 14 行 v0.2.0 状态）

**实现要点**

- 先把 `chore/scaffold-baseline`（`d95dc9b`）合入 `main`。main 是 `ee97ccc` 的祖先，
  可快进合并；**由你执行合并动作**，本片只负责在合并后的 main 上落文档改动。
- 不动任何 `packages/**` 代码。
- 追踪表的数字改动必须与实测一致：语句 / 行 94.51%、分支 90.83%、函数 94.05%。
  不要抄本文件的数字，重跑一次 `pnpm run test:coverage` 取当时的真值。
- README 的 tools 区：`upstream-watch`、`core-test-runner` 保留但标注「规划中」，
  另补一行 `webcrypto-probe/`（现只有一份 `probe.html`，ADR-013 的实测载体）。

**验收命令**

```bash
git log --oneline -1 main && pnpm run format:check && pnpm run check
```

**完成后更新的文档行**：本片即文档片，进度表 #0 标 Done + SHA。

---

### #1 `validator` 骨架：统一 `ValidationIssue`

**目标**：把 `YamlIssue` 与 `SchemaIssue` 收敛为单一 `ValidationIssue`，补齐 `module`、
`range`、`fix`。

**覆盖**：FR-VAL-01；NFR-SEC-03（消息不含配置值）。

**依赖**：#0。**必须是第一个代码切片**——版本文档「风险」表明确指出这项触及两个已有包的
公开 API，趁 v0.2.0 尚无外部使用者时改，越晚成本越高。

**涉及文件**

- 新建：`packages/validator/package.json`、`tsconfig.json`、`src/index.ts`、
  `src/issue.ts`、`src/issue.test.ts`
- 修改：`packages/yaml-engine/src/issues.ts`（`message` → i18n key 口径）
- 修改：`packages/schema-core/src/validate.ts`（`SchemaIssue` 对齐字段命名）
- 修改：根 `tsconfig.build.json`（加 `packages/validator` reference）

**实现要点**

- `ValidationIssue` 字段：`severity`、`code`（稳定机器码）、`module`（产出方标识：
  Schema 模块 id，或 `'yaml'` / `'reference'` / `'security'` 等内核阶段）、
  `messageKey` + 受限 `messageParams`、`path?`、`range?`、`fix?`、`blocking`。
- **`range` 的补齐位置是适配器，不是 `schema-core`。** `schema-core` 校验的是 JS 值，
  拿不到行列；让它依赖 `MihomoYamlDocument` 会倒置依赖方向。改为：`schema-core` 继续
  只产出 `path`，`@mcs/validator` 在适配时调用 `document.locate(path)` 补 `range`。
  这既满足版本文档「为 `SchemaIssue` 补上行列 `range`」，又不污染 `schema-core`。
- `fix` 是**声明式补丁描述**，不含任何可执行内容（ADR-002 / FR-UPD-07 的同一条底线）：
  `{ kind: 'set-scalar' | 'remove' | 'rename' | 'append', path, value? }`。`value` 只允许
  取自 Schema 的 `default` / `enum` / `const`，**不得回显用户当前值**（NFR-SEC-03）。
- `messageParams` 的类型要收窄到不可能塞进配置值：只接受 `ConfigPath`、字段名字符串、
  Schema 常量。这条要有否定用例。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-01 由 Partial 起草为「模型已统一，流水线见 #2」，
最终状态在 #2 后定；NFR-SEC-03 补本片的否定用例证据。

---

### #2 校验流水线、阶段编排与阻断判定

**目标**：把校验组织成可插拔的阶段流水线，并给出导出阻断判定。

**覆盖**：FR-VAL-01（流水线侧）、FR-YAML-07（阻断判定）、NFR-PERF-03（300 ms 防抖契约）。

**依赖**：#1。

**涉及文件**

- 新建：`packages/validator/src/pipeline.ts`、`src/pipeline.test.ts`
- 修改：`packages/validator/src/index.ts`

**实现要点**

- 阶段是数据而非硬编码：`ValidationStage { id, run(ctx): ValidationIssue[] }`，
  本版本只注册 `syntax`（来自 `hasSyntaxErrors` / `YamlIssue`）与 `schema`（占位，
  无模块时产出空集）。`reference` / `security` 阶段留 v0.3.0–v0.4.0 接入，
  **接口本版本定死**。
- 语法阶段失败时**短路**：后续阶段不跑（对 1 MB 非法输入不做无意义的全量校验）。
  这条要有用例。
- `blocking` 判定：语法错误恒为阻断；其余 `severity === 'error'` 为阻断。FR-YAML-07 的
  「只能导出无效草稿」由 `hasBlockingIssues(issues)` 单一函数决定，UI 不重新判断。
- 300 ms 防抖是**契约不是实现**：流水线本身同步纯函数，防抖归调度方（#10 的 Worker
  消息层）。本片只导出常量与说明，避免把定时器塞进纯函数包。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-01 → Done（指向 `issue.test.ts` + `pipeline.test.ts`）；
FR-YAML-07 由 Todo → Partial（判定已就绪，导出 UI 在 #15）。

---

### #3 `config-model` 统一历史栈（撤销 / 重做）

**目标**：在 `config-model` 层建立单一历史栈，表单编辑与引用改名走同一条栈。

**覆盖**：FR-PROJ-04；FR-REL-01/02/03/05 从 spike 转正（接入历史栈）。

**依赖**：#0。

**涉及文件**

- 新建：`packages/config-model/src/history.ts`、`src/history.test.ts`
- 新建：`packages/graph/src/history-integration.test.ts`（原计划未列，见下方「执行时修正」）
- 修改：`packages/config-model/src/index.ts`

**执行时修正**：`graph/tsconfig.json` 已 `references` → `config-model`（`graph` 依赖
`config-model` 取 `Entity`/`parseRuleLine`）。若 `config-model` 反过来在测试里
`import` `@mcs/graph` 以驱动真实的 `ReferenceIndex.rename()`，会在 `tsconfig.json`
形成循环 project reference，`tsc -b` 直接拒绝构建。因此拆成两层证据：
`history.test.ts` 只用手写的多路径 `document.setScalarIn` 序列证明**通用**的
"一次 `record()` 内多处改动 = 一个历史项"机制（不依赖 `graph`）；`graph` 已经单向
依赖 `config-model`，可以安全导入 `HistoryStack`，真正驱动 `ReferenceIndex.rename()`
级联改名的端到端证据放在新文件 `graph/src/history-integration.test.ts` 里。两层
证据合起来覆盖的断言与原计划完全一致，只是测试文件的物理位置因依赖方向调整。

**实现要点**

- **栈建在 `config-model` 而非 UI 层**——版本文档点名的理由：否则 v0.4.0 的批量操作
  无法并入。这条不可妥协。
- 历史项存的是**文本快照 + 操作描述**，不是 AST diff。理由：`MihomoYamlDocument` 是
  可变对象且 `commit()` 返回新实例，存文本最简单且天然可序列化（#5 的快照直接复用）。
  1 MB × 50 的内存代价在 #5 用裁剪策略解决，不在这里预优化。
- **合并规则**：连续同路径标量编辑在 N ms 内合并为一项（避免逐字符入栈）；
  一次级联改名（`graph` 的 `ReferenceIndex.rename()` 可能改多行）**必须是单个历史项**
  ——撤销一次回到改名前，不是回到改名的中间态。这是本片的核心断言，通用机制在
  `history.test.ts` 验证，真实级联改名场景在 `history-integration.test.ts` 验证。
- 撤销边界用例：空栈撤销、重做后新编辑截断重做分支、栈满后最旧项淘汰。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-PROJ-04 → Partial（内核就绪，UI 在 #11 之后）；
FR-REL-01/02/03/05 的「证据」列补历史栈接入用例。

---

### #4 `storage` 端口、内存与 IndexedDB 适配

**目标**：定义存储端口，给出 Web（IndexedDB）与内存两个实现，并定义 Android 适配层接口。

**覆盖**：NFR-REL-01；FR-PROJ-02 的持久化底座；兑现 `schema-registry` 的 `BundleStore` TODO。

**依赖**：#0。

**涉及文件**

- 新建：`packages/storage/package.json`、`tsconfig.json`、`src/index.ts`、
  `src/adapter.ts`、`src/memory.ts`、`src/indexeddb.ts`、`src/indexeddb.test.ts`、
  `src/memory.test.ts`
- 新建：`packages/schema-registry/src/storage-bridge.ts`、`src/storage-bridge.test.ts`
  （原计划未列具体文件名，见下方「执行时修正」）
- 修改：`packages/schema-registry/package.json`（加 `@mcs/storage` 依赖）、
  `packages/schema-registry/tsconfig.json`（加 `../storage` reference）、
  `packages/schema-registry/src/index.ts`（导出 `bundleStoreFrom`）、
  `packages/schema-registry/src/store.ts`、`src/testing/memory-store.ts`
  （消掉两处 v0.2.0 TODO 注释）
- 修改：根 `tsconfig.build.json`、根 `package.json`（`fake-indexeddb` devDependency）

**执行时修正**：`bundleStoreFrom` 桥接的两种类型（`StorageAdapter`、`BundleStore`）
分属两个包，函数本体只能二选一存放。放进 `packages/storage` 需要它反向 `import`
`@mcs/schema-registry` 才能拿到 `BundleStore`/`StoredBundle` 类型——但"什么是一个
Bundle"是领域知识，`storage` 应该是不认识任何领域概念的通用基础设施层，让它认识
"Bundle" 就把分层做反了。因此改为放进 `packages/schema-registry`（新文件
`storage-bridge.ts`），`schema-registry` 正向依赖 `@mcs/storage`，方向正确。

**实现要点**

- 端口：`StorageAdapter { get / put / delete / list(prefix) / estimateQuota? }`，
  值为 `Uint8Array`（不是字符串——`.mcsproj` 是二进制）。异步全 Promise。
- **Android 适配层只定义接口，不实现**（版本文档明确：Android 打包属 v0.6.0，但接口
  在本版本定义）。以 `AndroidFileAdapter extends StorageAdapter` 的形式声明所需的
  SAF / 私有目录语义差异，附 v0.6.0 的 TODO 引用，不写 Capacitor 调用
  （`packages/**` 已被 ESLint 禁止 import `@capacitor/*`）。
- IndexedDB 实现放在 `packages/storage` 而非 `apps/web`：IndexedDB 是浏览器 API 不是
  `node:fs`，不触犯 ESLint 规则；测试用 `fake-indexeddb`（新增 devDependency）。
- 顺带交付 `bundleStoreFrom(adapter)`：把 `schema-registry` 的 `BundleStore` 接到真实
  持久化上，消掉 `store.ts:11-13` 与 `memory-store.ts:3` 的两处 v0.2.0 TODO 注释。
- 配额探测（`estimateQuota`）为 #5 的降级策略服务，本片只给接口 + 内存实现的桩。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 NFR-REL-01 → Partial/Done（视用例覆盖）；
README 仓库结构 `storage/` 行加 ✅。

---

### #5 自动保存、快照与空间裁剪策略

**目标**：编辑后 5 秒内落盘，崩溃可恢复；快照至少保留 50 个，空间不足时按策略降级。

**覆盖**：FR-PROJ-02、FR-PROJ-05；NFR-REL-02、NFR-REL-05。

**依赖**：#3（历史项即快照来源）、#4（落盘）。

**涉及文件**

- 新建：`packages/storage/src/autosave.ts`、`src/autosave.test.ts`
- 新建：`packages/storage/src/snapshots.ts`、`src/snapshots.test.ts`

**实现要点**

- 自动保存是**注入时钟的纯调度器**，不自己 `setTimeout`：`AutoSaver { touch(now), flush() }`，
  由调用方喂时间。这样 5 秒边界可以用假时钟精确断言，不需要真实等待。
- **核心断言（NFR-REL-02）**：t=0 编辑、t=4.9s 模拟崩溃 → 重建 store 后读到的是上一次
  已落盘内容；t=5.1s 模拟崩溃 → 读到最新内容。「最多丢失 5 秒」是这条断言的字面含义。
- 浏览器侧的 `beforeunload` / `visibilitychange` 强制 flush 属 `apps/web`（#11 接线），
  本片只导出 `flush()`。
- 裁剪策略（FR-PROJ-05 + NFR-REL-05）分三级，逐级降级并各有用例：
  1. 快照数 > 50 → FIFO 淘汰最旧；
  2. `estimateQuota` 报告吃紧或 `put` 抛 `QuotaExceededError` → 把保留数压到下限；
  3. 仍然写不进 → 停止快照、只保当前文档，并产出一个可供 UI 提示的降级信号
     （信号是 i18n key + 结构化数据，不含配置值）。
- 「空间不足」用注入的失败型 adapter 模拟，不依赖真实浏览器配额。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-PROJ-02 → Partial（内核 Done，UI 接线在 #11）、
FR-PROJ-05 → Done、NFR-REL-02 / NFR-REL-05 → Done（指向 `autosave.test.ts`、`snapshots.test.ts`）。

---

### #6 `project-format`：`.mcsproj` ZIP 往返

**目标**：`.mcsproj` 便携项目包的读写，往返字节一致。

**覆盖**：FR-PROJ-06；NFR-SEC-08 的判定侧（提示文案在 #15）。

**依赖**：#4。

**涉及文件**

- 新建：`packages/project-format/package.json`、`tsconfig.json`、`src/index.ts`、
  `src/zip.ts`、`src/zip.test.ts`、`src/mcsproj.ts`、`src/mcsproj.test.ts`
- 新建：`docs/adr/ADR-017-mcsproj-container.md`（或按 #7 占号后顺延）
- 修改：根 `tsconfig.build.json`、`docs/adr/README.md`

**实现要点**

- **零依赖手写 ZIP**（决策 D2）：local file header `0x04034b50` / central directory
  `0x02014b50` / EOCD `0x06054b50`，压缩走平台内置 `CompressionStream('deflate-raw')`
  与 `DecompressionStream`（Node 20 与现代浏览器均有，无需 polyfill）。CRC-32 自算。
- **确定性字节输出**是本片的关键设计，直接决定往返能不能用「字节相等」断言：
  固定条目顺序、固定 DOS 时间戳常量、不写 extra field、不写 data descriptor。
  同一份项目导出两次必须逐字节相同——这要有用例。
- 容器内容按版本文档：`manifest.json`（元数据 + 格式版本）、`config.yaml`、
  `ui-state.json`（折叠状态、内部实体 ID 等非 Mihomo 数据）、`schema-lock.json`。
- **`config.yaml` 必须原样存取**：进容器前不重新序列化，出容器后交给
  `MihomoYamlDocument.parse` 之前不做任何规范化——否则 M0-1 的无损性在项目包这一层
  被悄悄破坏。用 `test-fixtures` 的 golden 用例做端到端往返断言。
- `describeSensitivity(project)`：返回哪些段可能含敏感数据（订阅 URL、密码、UUID、
  证书私钥），**只返回结构化判定，不返回文案**，文案由 #15 的 i18n key 渲染。
- ADR 记录：为什么手写而不引 fflate（与 ADR-008 自建校验器同一条供应链姿态），
  以及确定性输出的具体约定。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-PROJ-06 → Partial（往返 Done，导出入口在 #15）；
NFR-SEC-08 由 Todo → Partial；README 仓库结构 `project-format/` 行加 ✅；ADR 索引新行。

---

### #7 `packages/ui` 设计令牌与文字色层

**目标**：把 `DESIGN.md` 的令牌体系落为 TS 导出 + CSS 自定义属性，并补齐 ADR-011 未覆盖的
文字承载色。

**覆盖**：ADR-011 §1 / §2 / §5；NFR-A11Y。

**依赖**：#0。

**涉及文件**

- 新建：`packages/ui/package.json`、`tsconfig.json`、`src/index.ts`、`src/tokens.ts`、
  `src/tokens.test.ts`
- 新建：`docs/adr/ADR-017-text-bearing-color-layer.md`
- 修改：根 `tsconfig.build.json`、`docs/adr/README.md`

**实现要点**

- **单一来源**：CSS 自定义属性由 TS 常量生成（导出一个 `cssVariables()` 字符串），
  不手写两份，否则必然漂移。
- **每个令牌显式标注来源**（ADR-011 §5 硬要求）：`source: 'design-system' | 'mcs-extension'`。
  后续贡献者据此判断哪些能改。
- **文字色层（决策 D1，本片的实质内容）**：`DESIGN.md` 的调色板是**填充层**，
  新增一层 `text-*` 角色令牌，要求在 `canvas` #faf9f5 与 `surface-card` #efe9de 上
  **都** ≥ 4.5:1。已复算并留出余量的候选值：

  | 角色           | 值        | on canvas | on surface-card | 白字 on 该色 |
  | -------------- | --------- | --------- | --------------- | ------------ |
  | `text-primary` | `#98502f` | 5.66      | 4.93            | 5.96         |
  | `text-error`   | `#a63838` | 6.14      | 5.36            | 6.47         |
  | `text-warning` | `#7d5c0d` | 5.85      | 5.10            | 6.16         |
  | `text-success` | `#356b45` | 5.97      | 5.20            | 6.29         |
  | `text-info`    | `#33608f` | 6.21      | 5.41            | 6.54         |
  | `text-muted`   | `#64625c` | 5.79      | 5.05            | 6.10         |
  | `text-teal`    | `#356b61` | 5.82      | 5.07            | 6.13         |

  `text-info` 是新引入的蓝系——原调色板没有 info 色，而 FR-VAL-01 要 Error/Warning/Info
  三级。

- **严重级徽章的浅底**（配套，已复算）：`error-tint` #f7e8e5、`warning-tint` #f7f0dd、
  `success-tint` #e8f2e8、`info-tint` #e7eef7。对应 `text-*` 在其上为 5.43 / 5.42 /
  5.48 / 5.60，`body` 在其上 ≥ 9.15，`text-muted` 在其上 ≥ 5.12，全部达 AA。
- **填充层标注**：`primary` #cc785c、`warning` #d4a017、`success` #5db872、`accent-teal`、
  `accent-amber` 标记为 `fillOnly: true`（白字对比度分别只有 3.28 / 2.38 / 2.45，
  承载不了文字）。`primary-active` #a9583e（白字 5.06）、`error` #c64545（白字 4.84）、
  `surface-dark` #181715（白字 17.91）可承载白字。**ADR-011 §2 的结论不变、不改写**，
  ADR-017 是在其之上加一层，不是 supersede。
- 同时落地 ADR-011 §5 的扩展令牌：密度尺度、表单状态全集（error/warning/disabled/
  readonly）、亮暗主题对、hover 规范。
- 本片**不写组件**，只写令牌与类型。组件族随 #11 起按需长出。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：ADR 索引新增 ADR-017；追踪表 NFR-A11Y 的「三处对比度不达标修正」
缺口更新为「已修正，断言见 #8」。

---

### #8 对比度断言与 `DESIGN.md` 迁移

**目标**：令牌组合不达 AA 即 fail；把设计文档迁到 `docs/`。

**覆盖**：ADR-011 §6；退出条件「CI 对比度断言通过」。

**依赖**：#7。

**涉及文件**

- 新建：`packages/ui/src/contrast.ts`、`src/contrast.test.ts`
- 移动：`DESIGN.md` → `docs/design-system.md`
- 修改：`.prettierignore`（`DESIGN.md` 条目 → `docs/design-system.md`）
- 修改：`README.md`、`docs/adr/ADR-011-visual-design-system.md` 中的路径引用（如有）

**实现要点**

- `contrast.ts` 实现 WCAG 2.x 相对亮度与对比度公式（约 20 行，无依赖）。
- 断言是**表驱动**的：一张 `(前景令牌, 背景令牌, 最低比值)` 表，逐行断言。新增令牌
  必须进表，否则表的覆盖性用例失败——这条是防止后续悄悄绕过的关键。
- 表至少覆盖：全部 `text-*` × `{canvas, surface-card, 对应 tint}`；白字 × 全部
  非 `fillOnly` 填充色；`on-dark` / `on-dark-soft` × 暗面。
- **不新增独立 CI job**：断言以 vitest 用例形式存在，`pnpm run test` 已在 CI 的 check
  job 中运行，版本文档「CI 增加对比度断言」的要求即已满足。少一个 job 就少一个维护点。
  这条判断写进本片的提交信息，避免以后被误以为漏做。
- `.prettierignore` 迁移后必须验证：`docs/design-system.md` 仍不被格式化
  （它是外部来源文档，按原样保留）。跑 `pnpm run format:check` 确认。

**验收命令**

```bash
pnpm run check && pnpm run format:check && git log --follow --oneline -1 docs/design-system.md
```

**完成后更新的文档行**：追踪表 NFR-A11Y → Partial（对比度已断言，WCAG 全面走查与拖拽
键盘替代仍缺）；README 文档区补 `docs/design-system.md` 链接。

---

### #9 `apps/web` 脚手架、路由与 i18n 资源结构

**目标**：可运行的 Web 应用骨架，i18n 从第一行代码起齐全。

**覆盖**：PRD §11.6 i18n；ADR-016；NFR-SEC-07 的 CSP 落点（本片只搭，不做完整加固）。

**依赖**：#7。

**涉及文件**

- 新建：`apps/web/package.json`、`tsconfig.json`、`vite.config.ts`、`index.html`、
  `src/main.tsx`、`src/App.tsx`、`src/i18n/{index.ts,zh-CN.json,en.json,i18n.test.ts}`
- 修改：根 `tsconfig.build.json`、`vitest.config.ts`、`package.json`（`dev` / `build` 脚本）

**实现要点**

- Vite + React 19 + TS。React 与 react-dom 已在根 devDependencies，`apps/web` 声明为
  依赖即可。
- **i18n 自建，不引 i18next**：本版本只需要 `t(key, params)` 查表 + 语言切换，约 40 行。
  引一个重库换 40 行代码不划算，且 ADR-016 要求的是**资源结构**齐全而非库特性。
- **ADR-016 明确指派给本版本的强制检查**：「两份资源文件的 key 集合必须相等」。
  `i18n.test.ts` 断言 `zh-CN.json` 与 `en.json` 的 key 集合完全相等（双向差集为空），
  且没有空字符串值。这是 ADR-016 里唯一一条留给「引入 apps/web 时一并实现」的待办，
  必须在本片兑现。
- 路由：引入 `react-router-dom`。本版本只有 2–3 个路由，但 v0.3.0 表单页与 v0.4.0 关系图
  会持续增加；自建 hash 路由省一个依赖但要自己维护嵌套路由与懒加载。
  **备选**：若你不想加这个依赖，改为自建 hash 路由（约 60 行），本片实现成本相当。
- **vitest 配置改动（口径问题，务必按此执行）**：
  - `test.include` 增加 `apps/web/src/**/*.test.ts` 与 `*.test.tsx`；
  - `coverage.include` 增加 `apps/web/src/**/*.ts` 与 `*.tsx`；
  - `coverage.exclude` 只增加 `apps/web/src/main.tsx`（bootstrap 入口，无逻辑）；
  - **阈值一个数字都不动**。把 UI 纳入覆盖率统计是「不得放宽」的自然读法，
    不是额外要求。
- CSP：`index.html` 先落一份不含 `unsafe-eval` 的 meta CSP（ADR-008 已保证校验器
  不需要 eval），完整加固与部署头留 v0.6.0 / v0.9.0。

**验收命令**

```bash
pnpm run check && pnpm run format:check && pnpm --filter @mcs/web exec vite build
```

**完成后更新的文档行**：追踪表 §11 i18n 行补证据；README 仓库结构 `apps/web/` 行加 ✅、
本地开发区补 `pnpm run dev`。

---

### #10 Worker 边界：解析 / 校验 / 差异出主线程

**目标**：把 YAML 解析、全量校验、差异计算固定在 Web Worker 内。

**覆盖**：NFR-PERF-05、NFR-PERF-03（300 ms 防抖在此实现）。

**依赖**：#2、#9。

**执行时修正**：原「涉及文件」只列了 `worker/` 下的五个新文件，但 `apps/web` 在此之前
从未依赖过 `@mcs/yaml-engine` / `@mcs/validator`，实现时发现还需要两处未列出的登记性
修改：(a) `apps/web/package.json` 加两个 `workspace:*` 依赖、`apps/web/tsconfig.json`
的 `references` 加对应两条路径——这与「新增包必须登记」是同一条工程约束的自然延伸，
只是这次是「已有 app 首次依赖已有 package」而非「新增 package」；(b)
`vitest.config.ts` 的 `coverage.exclude` 加一行 `config.worker.ts`——它是真实 Worker
入口（`self.onmessage` 接线），在 jsdom/node 测试环境下无法有意义地执行，与 #9 排除
`main.tsx` 的理由完全一致（纯 bootstrap，自身无逻辑；真正的处理逻辑被抽到 `protocol.ts`
的 `handleWorkerRequest`，由 `protocol.test.ts` 直接调用，不依赖任何 Worker 全局对象）。

**涉及文件**

- 新建：`apps/web/src/worker/config.worker.ts`、`src/worker/protocol.ts`、
  `src/worker/client.ts`、`src/worker/protocol.test.ts`、`src/worker/client.test.ts`
- 修改：`apps/web/package.json`、`apps/web/tsconfig.json`（登记 `@mcs/yaml-engine`、
  `@mcs/validator` 依赖与 project reference）；`vitest.config.ts`（`coverage.exclude`
  加 `config.worker.ts`）

**实现要点**

- **版本文档的风险表已经把设计定死了，照做**：Worker 内持有 `MihomoYamlDocument`，
  主线程**只传路径与补丁，绝不传 AST**。结构化克隆一个 1 MB 文档的 AST 会把性能收益
  吃光。
- 消息协议是**可穷举的判别式联合**：`parse` / `applyPatch` / `validate` / `diff` /
  `serialize` 各一对请求响应。协议类型定义在 `protocol.ts`，主线程与 Worker 共用，
  改协议必然两侧同时改。
- **归属断言（决策 D3 下的验证手段）**：一条静态用例断言主线程模块图里不出现
  `@mcs/yaml-engine` / `@mcs/validator` 的直接 import——引擎只能经 `client.ts` 的
  postMessage 到达。这把「运行在 Worker 中」变成可自动化的结构性事实，而不是靠肉眼。
  真实浏览器的 long-task 测量留 v0.9.0（其版本文档本就以 E2E 为主题）。
- 300 ms 防抖在 `client.ts`：连续编辑只触发一次全量校验，且**最后一次编辑必定被校验**
  （不能被防抖吞掉）。这条要有用例。
- 单测用假 Worker（实现 `postMessage` / `onmessage` 的对象）驱动，不依赖真实
  `new Worker()`。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 NFR-PERF-05 → Done（指向 `client.test.ts` 的归属断言与
防抖用例）、NFR-PERF-03 → Partial（防抖 Done，字段编辑 <100 ms 待 v0.3.0 有表单后测）。

---

### #11 三栏布局壳与项目管理页

**目标**：三栏布局壳 + 项目的创建、命名、目标兼容档案、删除二次确认。

**覆盖**：FR-PROJ-01、FR-PROJ-03；FR-PROJ-02 / FR-PROJ-04 的 UI 接线。

**依赖**：#5、#9。

**执行时修正**：原计划「接线 #3 的历史栈」一句隐含假设 UI 层能直接持有一个
`MihomoYamlDocument` 来调用 `HistoryStack.record()`。但 #10（与本片同为 #9 的并列
下游、彼此不互相依赖）已经把唯一的 `MihomoYamlDocument` 实例锁进 Worker 内，主线程
「只传路径与补丁，绝不传 AST」——这条边界是 #10 的结构性断言，本片不应该、也不能在
`apps/web` 主线程里反过来直接 `import '@mcs/yaml-engine'` 建一份文档来满足
`HistoryStack.record()` 的签名，否则就打破了刚建立的 Worker 归属断言。同时，#12/#13
（真正的 YAML 导入与编辑器）此时都还没有落地，本片也没有任何用户可触发的文档级编辑
动作——历史栈此刻确实无事可记。

真正让"撤销/重做覆盖表单编辑"成立，需要 #13 给 Worker 协议加 `undo`/`redo` 一对新
消息类型（`config.worker.ts` 内持有 `HistoryStack`，与它已持有的 `MihomoYamlDocument`
放在一起），由 `WorkerClient` 转发——这是 #13 的自然延伸，不属于本片"涉及文件"列出的
纯 `apps/web` 表层改动。本片改为只交付撤销/重做的**壳层**：工具栏按钮 + 快捷键
（Ctrl+Z / Ctrl+Shift+Z），从 `@mcs/config-model` 真实导入 `HistoryStack`（不是假的
占位类型），`canUndo`/`canRedo` 正确驱动按钮禁用态，用注入的栈实例证明这套接线本身
是对的——但没有任何路径能让它在本片的实际浏览器操作中被真正触发（栈永远是空的）。
追踪表 FR-PROJ-04 因此**不**标 Done，维持 Partial，缺口写清楚指向 #13。

`目标兼容档案默认值`同理改为最小化处理：ADR-012 的落点表写的是
`CompatibilityProfile.coreRange`，但这个类型在代码库中从未被建过（全仓库
grep 无匹配）——建出完整类型是未被任何切片排期的新范围。本片改为按计划原文
「或一个明确的常量导出」这一备选项，在 `apps/web/src/project` 内新增一个具名常量
（不进 `packages/config-model`/`schema-registry`，避免又一处未列出的跨包改动），
注释直接引用 ADR-012 作为数值来源。

**涉及文件**

- 新建：`apps/web/src/layout/AppShell.tsx`、`src/layout/AppShell.test.tsx`
- 新建：`apps/web/src/project/ProjectPage.tsx`、`src/project/ProjectPage.test.tsx`
- 修改：`apps/web/src/App.tsx`、`src/App.test.tsx`、i18n 两份资源

**实现要点**

- 目标兼容档案默认 v1.19.29（ADR-012），来自 `apps/web/src/project` 内的具名常量，
  不是组件里的字面量（见上「执行时修正」）。
- 删除二次确认（FR-PROJ-03）：确认对话框必须说明「支持导出后删除」这条路径，
  文案走 i18n key。
- 接线 #5 的 `flush()`：项目名称/描述编辑走 `AutoSaver`（`touch`/`flush`，纯元数据、
  不触碰 `@mcs/yaml-engine`）；`visibilitychange` → hidden 与 `beforeunload` 时强制
  落盘，切换选中项目前也强制落盘。
- 撤销 / 重做：壳层按钮 + 快捷键，接 `@mcs/config-model` 的真实 `HistoryStack`，
  禁用态由栈状态决定；本片栈始终为空（见上「执行时修正」），真正的文档级记录留
  给 #13。
- 三栏布局用 CSS Grid + #7 的断点令牌；本片不做窄屏折叠的完整响应式（Android 形态
  属 v0.6.0），但断点令牌要用上、不写 magic number。
- 全部文本走 i18n key；新增 key 必须中英同步（#9 的断言会拦）。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-PROJ-01 → Done、FR-PROJ-03 → Done、
FR-PROJ-02 → Done；FR-PROJ-04 维持 Partial（壳层 UI 就绪，文档级记录待 #13 扩展
Worker 协议后再转 Done）。

---

### #12 YAML 导入入口（文件 / 粘贴 / 剪贴板）

**目标**：`.yaml` / `.yml` 文件与纯文本粘贴两条导入路径。

**覆盖**：FR-YAML-01；NFR-REL-04。

**依赖**：#10、#11。

**执行时修正**：原「涉及文件」写「修改 AppShell.tsx」，但 #11 已把 AppShell 定死为
**纯结构、不认识"项目"这个概念**的壳层（侧栏/主内容/辅助面板三个插槽，无业务逻辑）；
导入天然是"把内容导入*当前选中项目*"的动作，只有 `ProjectPage.tsx` 持有"当前选中
项目"这个状态。因此改为修改 `ProjectPage.tsx`（渲染 `<ImportPanel>` 并提供
`onImport` 回调，写入被导入项目的 `config.yaml`），不动 `AppShell.tsx`。

由此连带两处此前未列出的改动：

1. `apps/web/src/worker/client.ts` 新增 `createConfigWorkerClient()`——#10 的实现
   笔记里就写明"真实 `new Worker()` 的接线留给第一个消费它的切片"，本片正是那个
   消费者。工厂函数只做 `new Worker(...)` + 适配 `WorkerLike`，不引入新的
   `@mcs/yaml-engine`/`@mcs/validator` 引用，不影响 #10 的边界断言。
2. `ProjectPage.tsx` 新增必填 prop `client`（承载解析能力），与 `adapter` 同一套
   理由——真实 Worker 在非浏览器/无 Worker 支持的环境构造会抛错，因此不给内部
   默认值，由 `App.tsx` 懒构造后注入，测试用假 client 注入。连带
   `ProjectPage.test.tsx` 里已有的 `render(<ProjectPage ... />)` 调用点统一补上
   这个 prop。

**涉及文件**

- 新建：`apps/web/src/import/ImportPanel.tsx`、`src/import/ImportPanel.test.tsx`
- 修改：`apps/web/src/project/ProjectPage.tsx`、`src/project/ProjectPage.test.tsx`、
  `apps/web/src/worker/client.ts`、`apps/web/src/App.tsx`、i18n 两份资源

**实现要点**

- 三条入口：文件选择、拖放、纯文本粘贴框。文件读取用 `File.text()`，不碰
  `node:fs`（在 `apps/web` 也不该出现）。
- **NFR-REL-04（导入不覆盖原文件）在本片是设计而非代码**：导入只读取内容进项目存储，
  应用**从不持有原文件句柄**，因此不存在覆盖路径。用例断言导入流程中没有任何写回
  文件系统的调用；同时在代码注释里点明这条约束的来源，避免后续有人加「保存回原文件」。
- 超限输入（`DEFAULT_YAML_LIMITS`）要给出可读提示而不是崩溃，提示走 i18n key 且
  **不回显文件内容**（NFR-SEC-03）。
- 解析在 Worker（#10），主线程只发文本、收结果摘要；成功与否由 `hasBlockingIssues()`
  单一函数判定（复用 #10 的判定点，不在本片重新发明规则）。逐条问题的文案渲染
  （`messageKey` → 具体文案）留给 #14 的问题中心，本片只给一句通用的成功/失败提示。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-YAML-01 → Done、NFR-REL-04 → Done。

---

### #13 原文编辑器与非法时冻结结构化视图

**目标**：行号、语法错误标记、查找、格式化、跳转；YAML 非法时冻结结构化视图并提示修复。

**覆盖**：FR-YAML-04、FR-YAML-05。

**依赖**：#12。

**执行时修正**：

1. 「修改 AppShell.tsx」再次不成立，原因与 #12 相同且更明确——AppShell 不认识
   "项目"，而编辑器编辑的是*某个项目*的 `config.yaml`。改为修改
   `apps/web/src/project/ProjectPage.tsx`（渲染 `<YamlEditor>`，接线其
   `text`/`onChange` 到该项目的 config 内容）与 `apps/web/src/project/model.ts`
   （补一个此前没有的读取函数 `getProjectConfigText`——之前只有写入
   `saveProjectConfigText`，因为在本片之前没有任何 UI 需要把 config 内容读回来
   显示）。
2. 「跳转定位用引擎侧已有的 `locate()`」——`locate()` 是 `MihomoYamlDocument` 的
   实例方法，只存在于 Worker 内部（#10 的边界），协议里没有对应的消息类型，
   而本片也没有"点击某个 Schema 字段跳到对应行"这种触发源（那是 #14/v0.3.0
   有了结构化表单之后的事）。本片唯一需要的跳转场景是"点击冻结提示，跳到第一个
   语法错误"——这个位置信息已经在 `parse()` 返回的 `ValidationIssue.range` 里
   （`offset`/`line`/`column` 都有），不需要额外发一次 `locate` 消息。因此本片
   不新增协议消息类型，`locate()` 的协议化留给真正需要按路径跳转的切片。
3. 「结构化视图」在 v0.2.0 里没有具体所指——schema 驱动表单（`packages/form-renderer`）
   要到有 Schema 模块（v0.3.0+）才会接入 `apps/web`，#17 的收口用例还要求"不装
   任何 Schema 模块也能走完整个闭环"。本片因此把"结构化视图"实现为
   `YamlEditor.tsx` 内部一个占位区块（`<fieldset>`，语义化的可禁用容器），
   冻结逻辑真实生效、可测试，只是占位内容是一句说明文字而非真表单——留给
   v0.3.0 表单接入时直接把内容换掉，不用碰冻结机制本身。
4. 「格式化调用 `toText({lineWidth, indent})`」按计划实现，但**格式化在未发生过
   结构性编辑的文档上是预期的 no-op**：`MihomoYamlDocument` 在 `cst` 模式下
   `toText()`会完全忽略传入的 `SerializeOptions`、逐字节回放原始 token（这正是
   M0-1 无损往返的机制），只有发生过结构性修改（`setIn`/`deleteIn`/`appendIn`
   等）才会转入 `ast` 模式、`SerializeOptions` 才生效。本片的编辑器只做原文
   文本编辑，不调用任何结构化修改方法，因此点击"格式化"对刚导入的文档通常不会
   看见变化——这不是 bug，是无损往返保证的直接推论，已在实现注释与用例里写清楚。

**涉及文件**

- 新建：`apps/web/src/editor/YamlEditor.tsx`、`src/editor/YamlEditor.test.tsx`
- 修改：`apps/web/src/project/ProjectPage.tsx`、`src/project/ProjectPage.test.tsx`、
  `apps/web/src/project/model.ts`、i18n 两份资源

**实现要点**

- **编辑器不引 CodeMirror / Monaco**。本版本要的是行号 + 错误标记 + 查找 + 跳转，
  用 `<textarea>` + 行号槽 + 覆盖层即可；引一个重编辑器会连带引入它自己的 CSP、
  Worker 与主题体系，与 #7 的令牌层和 #10 的 Worker 边界都要打架。
  **若后续证明性能或体验不达标，再单独开 ADR 换实现**——记在本片的实现注释里。
- 格式化调用 `MihomoYamlDocument.toText({ lineWidth, indent })`（`SerializeOptions`
  已有），不自己写格式化器（见上「执行时修正」第 4 条的 no-op 说明）。
- 跳转定位用 `parse()` 已返回的 `ValidationIssue.range`（见上「执行时修正」第 2 条）。
- 编辑器内部按 300ms（`VALIDATION_DEBOUNCE_MS`，复用 #10/#2 的同一常量）防抖调用
  `client.parse(text)`，用 React `useEffect` + `setTimeout`（不是 `WorkerClient`
  的注入时钟 `touchValidate`/`pollValidate`——那一套是给非 React 的纯类设计的，
  编辑器是 React 组件，用标准的 effect 防抖 + `vi.useFakeTimers()` 测试更符合
  这一层的写法）。
- **FR-YAML-05 冻结逻辑**：`hasSyntaxErrors`（本片等价于 `hasBlockingIssues(issues)`，
  因为 v0.2.0 没有 Schema 模块、schema 阶段恒为空——这条等价关系随 Schema 模块
  接入后需要重新审视，已在代码注释标注）为真时，结构化视图整体禁用并展示修复提示；
  **禁用不是隐藏**——用户要能看到冻结前的状态，否则会以为数据丢了。这条要有用例：
  「非法 YAML 时结构化视图不可交互但仍可见，且提示指向第一个语法错误的行列」。
- 大文档下行号槽不做全量渲染的性能优化留 v0.9.0（NFR-PERF-04 是 10,000 条规则，
  属 v0.4.0 之后的规则表），本片不预优化。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-YAML-04 → Done、FR-YAML-05 由 Partial → Done。

---

### #14 问题中心与点击跳转 YAML 行

**目标**：统一问题列表，点击问题跳转到对应 YAML 行。

**覆盖**：FR-VAL-02。

**依赖**：#13。

**执行时修正**：

1. 「跳转：range 缺失时回退到 document.locate(issue.path)」——`locate()` 是
   `MihomoYamlDocument` 的实例方法，只存在于 Worker 内（#10 的边界），协议里
   没有对应消息类型。#13 的执行时修正曾把这一步明确留给"真正需要按路径跳转的
   切片"——本片就是那个切片：新增协议消息 `locate`（`apps/web/src/worker/
protocol.ts` 的 `handleWorkerRequest` 加一个 case，`client.ts` 的
   `WorkerClient` 加一个 `locate()` 方法），不引入新的
   `@mcs/yaml-engine`/`@mcs/validator` 引用，不影响 #10 的主线程边界断言。
2. 「问题面板消费 #2 流水线产出的 ValidationIssue[]」没有说明这份数组从哪来。
   `YamlEditor`（#13）已经在自己内部做了防抖 `parse()`，如果 `IssuePanel` 再
   独立调一次，就是同一份文本解析两遍、且两个面板可能显示不一致的结果。改为
   `YamlEditor` 新增可选 prop `onIssuesChange`，把它已经算出来的 issues 上报给
   `ProjectPage`，`ProjectPage` 再把同一份数组喂给 `IssuePanel`——单一数据源，
   零重复解析。连带 `YamlEditor` 改用 `forwardRef` 暴露一个 `jumpToRange`
   方法（React 官方推荐的"父组件触发子组件内命令式动作"模式，用于把
   `IssuePanel` 的点击接到编辑器的 `selectRange`），`ProjectPage.tsx` 因此需要
   修改（涉及文件原计划没列，与 #12/#13 同一类"AppShell 不认识项目"的登记性
   缺口，不再重复展开）。
3. `client` prop 类型连续第三次需要因为新组件的需要而变宽（#12 引入
   `ImportWorkerClient`、#13 扩到 `YamlEditorWorkerClient`）。本片改为交叉类型
   `ImportWorkerClient & YamlEditorWorkerClient & IssuePanelWorkerClient`——
   每个组件继续声明自己最小的接口（测试假实现保持精简），`ProjectPage` 用 `&`
   自动合并，不再需要每片手动重写一遍"最宽形状"。
4. IssuePanel 渲染进 `AppShell` 的 `aside` 插槽——这正是 #11 建 `AppShell` 时
   注释里写的"reserved panel for the issue/diff surfaces #14 and #15 add
   later"，占位文案在本片被替换掉。`AppShell.tsx` 本身不用改（`aside` 插槽
   #11 就有），只是 `ProjectPage.tsx` 现在真正传值进去。

**涉及文件**

- 新建：`apps/web/src/issues/IssuePanel.tsx`、`src/issues/IssuePanel.test.tsx`
- 修改：`apps/web/src/editor/YamlEditor.tsx`、`src/editor/YamlEditor.test.tsx`、
  `apps/web/src/project/ProjectPage.tsx`、`src/project/ProjectPage.test.tsx`、
  `apps/web/src/worker/protocol.ts`、`src/worker/protocol.test.ts`、
  `apps/web/src/worker/client.ts`、`src/worker/client.test.ts`、i18n 两份资源

**实现要点**

- 消费 #2 流水线产出的 `ValidationIssue[]`（经 `YamlEditor` 上报，见上），按
  `severity` 分组、按 `module` 可筛。
- 严重级用 #7 的 `text-*` + `*-tint` + **图标**表达——PRD §11.6 要求错误不得只用颜色
  编码（ADR-011 §5 已列为扩展项）。用例断言每个严重级都有非颜色标记。
- 跳转：用 `issue.range` 定位；`range` 缺失时（纯路径问题）回退到 Worker 的
  `locate` 消息（见上「执行时修正」第 1 条）。两条路径都要有用例——v0.2.0 无
  Schema 模块时后一条路径不会被真实数据触发，用例里手工构造一个只有 `path`
  没有 `range` 的 `ValidationIssue` 驱动。
- 「跳转到表单字段」明确属 v0.3.0（版本文档已划出），本片只做跳转到 YAML 行。
- 文案由 `messageKey` + `messageParams` 渲染，**问题面板是 i18n key 到文案映射表的
  第一个真实消费者**（ADR-016 点名的 code→文案映射，中英同步）。`yaml.syntax.*`
  的具体错误码来自第三方 `yaml` 库、是开放集合，无法穷举预翻译；未命中的 key
  走 `t()` 已有的"回退到 key 本身"降级，不崩溃。已加真实翻译的是 5 个静态 key
  （`yaml.limit.size`/`yaml.parse.failed`/`yaml.parse.empty`/
  `yaml.limit.documents`/`yaml.limit.depth`）。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-02 → Done。

---

### #15 差异面板、导出与敏感数据提示

**目标**：导出前看差异；正常导出 `config.yaml` 与 `.mcsproj`；有阻断错误时只能导出无效草稿。

**覆盖**：FR-YAML-06、FR-YAML-07；FR-PROJ-06 的导出入口；NFR-SEC-08。

**依赖**：#6、#14。

**涉及文件**

- 新建：`apps/web/src/diff/DiffPanel.tsx`、`src/diff/DiffPanel.test.tsx`
- 新建：`apps/web/src/export/ExportDialog.tsx`、`src/export/ExportDialog.test.tsx`
- 修改：i18n 两份资源

**实现要点**

- 差异基准是「导入版本或最近保存版本」（版本文档原话），两者都要能选。`diffLines()`
  已实现，差异计算在 Worker（#10）。
- 增删色用 #7 的令牌，且**必须配符号**（`+` / `-`），不只靠颜色。
- **FR-YAML-07 的两条出口互斥**：`hasBlockingIssues()` 为真 → 主导出按钮禁用，
  只留「导出无效草稿」，且草稿导出必须有明确标识（文件名后缀或 manifest 标记），
  避免用户误以为拿到的是可用配置。这条要有用例。
- **NFR-SEC-08**：`.mcsproj` 导出与剪贴板复制前，用 #6 的 `describeSensitivity()`
  结果渲染提示，列出**哪几类**敏感数据可能存在（订阅 URL / 密码 / UUID / 证书私钥），
  **不显示具体值**。用例断言提示文本中不出现任何配置值。
- 导出走 Blob + `URL.createObjectURL`，不经任何网络路径。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-YAML-06 由 Partial → Done、FR-YAML-07 → Done、
FR-PROJ-06 → Done、NFR-SEC-08 → Done。

---

### #16 1 MB 语料性能基准

**目标**：1 MB YAML 的导入 + 解析 + 首轮校验 < 2 秒，有脚本与记录数字。

**覆盖**：NFR-PERF-02。

**依赖**：#10。

**涉及文件**

- 新建：`packages/test-fixtures/src/generate-large.ts`（生成器，非 golden 文件）
- 新建：`packages/validator/src/bench.test.ts` 或 `bench/import.bench.ts`
- 新建：`docs/releases/plans/v0.2.0-perf-baseline.md`（记录数字与方法）
- 修改：`.github/workflows/ci.yml`（基准 step，**只记录不阻断**）

**实现要点**

- **1 MB 语料用确定性生成器，不提交 1 MB 文件**。理由：`fixtures/**` 是字节敏感的
  golden 目录，塞一份 1 MB 生成物进去既污染 golden 语义又让每次 clone 变重。
  生成器用固定种子，同输入必得同输出，基准可复现。
- 生成的语料要**像真配置**：大量 rules、proxies、proxy-groups、注释与锚点，
  而不是一行重复 10 万次——否则测的是解析器的最好情况。
- 基准同时记录三段耗时：解析、首轮校验、总计。退出条件只卡总计 < 2s，但分段数字
  是后续优化的唯一抓手。
- **CI 只记录不阻断**（版本文档明确：阻断阈值留到 v0.9.0）。CI runner 性能波动大，
  这个阶段设阻断线只会制造假警报。
- 数字写进 `v0.2.0-perf-baseline.md`：机器型号、Node 版本、采样次数、中位数，
  格式参照 `v0.1.0-android-evidence.md`。

**验收命令**

```bash
pnpm run check && pnpm run format:check && pnpm vitest bench --run
```

**完成后更新的文档行**：追踪表 NFR-PERF-02 → Done（指向基准文件与记录文档）。

---

### #17 闭环验收与文档收口

**目标**：不装任何 Schema 模块，走通「导入 → 原文编辑 → 差异 → 导出」，并核对全部退出条件。

**覆盖**：版本文档「本版本要回答的问题」与全部退出条件。

**依赖**：全部。

**涉及文件**

- 新建：`apps/web/src/closed-loop.test.tsx`（端到端闭环用例，jsdom 级）
- 修改：`docs/requirements-traceability.md`、`docs/releases/README.md`、`README.md`
- 修改：`docs/releases/plans/v0.2.0.md`（本文件的进度表收口）

**实现要点**

- 闭环用例是**一条测试走完全程**：粘贴 YAML → 编辑一处 → 打开差异 → 导出，
  断言导出内容与预期逐字节一致，且**全程没有安装任何 Schema 模块**。这条用例
  就是版本文档开篇那个问题的答案，它绿了这个版本才算成立。
- 逐条核对退出条件（见下节映射表），不能核对通过的**不许标 Done，写明缺口**。
- 追踪表标 Done 必须能指向具体测试文件；指不出来的一律记 Partial。

**验收命令**

```bash
pnpm run check && pnpm run format:check && pnpm run test:coverage
```

**完成后更新的文档行**：追踪表全表复核；`docs/releases/README.md` v0.2.0 → 已完成；
README 项目状态与仓库结构。

---

## 退出条件映射

| 版本文档退出条件                                        | 由哪片保证                       | 证据形态                                            |
| ------------------------------------------------------- | -------------------------------- | --------------------------------------------------- |
| 不装 Schema 模块可完成「导入 → 原文编辑 → 差异 → 导出」 | **#17**（依赖 #12–#15）          | `closed-loop.test.tsx` 单条端到端用例               |
| 1 MB 导入解析 + 首轮校验 < 2s，有脚本与记录数字         | **#16**                          | bench 文件 + `v0.2.0-perf-baseline.md`              |
| 解析、校验、差异运行在 Worker，主线程无长任务           | **#10**                          | 主线程模块图归属断言 + 协议单测（结构性，见 D3）    |
| 自动保存与崩溃恢复有自动化验证，最多丢失 5 秒           | **#5**（UI 接线 #11）            | 假时钟 4.9s / 5.1s 双向断言                         |
| `.mcsproj` 导入导出往返一致，含敏感数据提示             | **#6**（往返）+ **#15**（提示）  | 字节相等往返用例 + 提示不含配置值用例               |
| 撤销重做覆盖表单编辑与引用改名，走同一历史栈            | **#3**（栈）+ **#11**（UI）      | 级联改名为单个历史项的断言                          |
| `ValidationIssue` 已统一，含 `module`、`range`、`fix`   | **#1**（模型）+ **#2**（流水线） | `issue.test.ts` + `pipeline.test.ts`                |
| `packages/ui` token 已落地，CI 对比度断言通过           | **#7**（令牌）+ **#8**（断言）   | 表驱动 `contrast.test.ts`，随 `pnpm run test` 进 CI |
| `pnpm run check` 与 `format:check` 全绿，覆盖率 ≥ 85%   | **每一片**，#17 终检             | 每片验收命令 + #17 的 `test:coverage`               |

版本文档「测试要求」的对应：单元（#4 存储适配、#5 快照裁剪、#6 `.mcsproj` 往返、
#3 历史栈合并与撤销边界）、Round-trip（#6 沿用 `test-fixtures` golden）、
性能（#16 纳入 CI 只记录不阻断）。

## 风险

| 风险                                             | 影响                                       | 缓解                                                                                                        |
| ------------------------------------------------ | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| #1 改动 `yaml-engine` / `schema-core` 公开 API   | 两个已 Done 的包共 129 例测试可能连带返工  | 排为第一个代码切片（版本文档风险表原话：越晚成本越高）；`message` → `messageKey` 是唯一破坏性改名，一次改完 |
| Worker 归属只有结构性断言，无真实 long-task 数据 | 「主线程无长任务」可能在真实浏览器里不成立 | D3 已定案留 v0.9.0；#16 的分段耗时能提前暴露解析/校验本身是否超预算                                         |
| 手写 ZIP 的边界情况（大文件、UTF-8 文件名）      | `.mcsproj` 在某些环境下打不开              | #6 只用固定的四个 ASCII 文件名；用例覆盖空文件、1 MB 文件、往返两次字节相等                                 |
| `apps/web` 纳入覆盖率后 UI 代码拉低总覆盖        | 85% 阈值可能在 #11–#15 期间反复红          | 每片自带用例、不攒到最后；`main.tsx` 已排除；若真顶不住，是补测试而不是调阈值                               |
| 自建 i18n 与路由的长期维护成本                   | v0.3.0 页面增多后可能要换库                | 两者接口都很小，换库是局部替换；#9 里把「为什么不引库」写进注释，方便后人重新评估                           |
| 不引重型编辑器（#13）                            | 大文档编辑体验可能不足                     | 明确记在 #13：若不达标，单开 ADR 换实现，不在本版本临时加库                                                 |
| `packages/ui` 令牌与扩展边界模糊（版本文档原列） | 后续贡献者不知道哪些能改                   | #7 的 `source` 字段是硬性标注；#8 的表覆盖性用例保证新令牌必须进断言表                                      |

## 已确认的决策

四项在规划阶段提出并已定案，实现时直接执行，不再重开讨论。

**D1 —— 对比度断言表的范围（→ #7、#8）**
补一层 `text-*` 文字承载色，断言表覆盖 `canvas` 与 `surface-card` 两个底色。
背景：ADR-011 §2 只算了 `canvas`，复算发现 `warning` #d4a017 作文字 = 2.25、
`success` #5db872 = 2.33、`accent-teal` = 2.25、`accent-amber` = 2.00；
在 `surface-card` #efe9de 上 `muted` = 4.48、`primary-active` = 4.19、`error` = 4.01，
全部不到 4.5——若不处理，退出条件「CI 对比度断言通过」必然 fail。
落地方式：新写 **ADR-017**，**不修改已 Accepted 的 ADR-011**。

**D2 —— `.mcsproj` 的 ZIP 实现（→ #6）**
零依赖手写 ZIP 读写，压缩走平台内置 `CompressionStream('deflate-raw')`。
与 ADR-008（自建 JSON Schema 校验器、不引第三方）同一条供应链姿态，且产物字节
确定性可控，往返一致性好断言。

**D3 —— 浏览器级证据的验证工具链（→ #5、#10、#16）**
本版本用 vitest + 假件（假 Worker、`fake-indexeddb`、假时钟），真 E2E 留 v0.9.0
——该版本文档本就以「内核测试矩阵、E2E、安全与性能」为主题。
**代价已知并接受**：「主线程无长任务」是结构性证明（主线程模块图不直接依赖引擎），
不是真实 timing 测量。这条要在 #17 的追踪表证据列里如实写明口径，不得含糊标 Done。

**D4 —— 分支基线（→ #0）**
先把 `chore/scaffold-baseline`（`d95dc9b`）合入 `main`，之后每个 `feat/v0.2.0-*`
都从 main 切出、合回 main。合并动作由你执行。
