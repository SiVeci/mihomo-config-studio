# v0.3.0 执行计划 —— Schema 表单与前半配置模块（Alpha）

需求来源：[v0.3.0 版本文档](../v0.3.0-schema-forms-alpha.md)。本文件只做拆解与排序，
不新增、不删减需求。两者冲突时以版本文档为准。

## 基线

| 项目                    | 实测值                                                                                                                                                     |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 生成日期                | 2026-08-18                                                                                                                                                 |
| 基线 commit             | `1b9304e`（docs: 标记切片 #17 为 Done 并记录提交 SHA），分支 `main`                                                                                        |
| 工作区状态              | 干净（`git status --short` 无输出）                                                                                                                        |
| `pnpm run check`        | **全绿**，exit 0（typecheck + lint + test）                                                                                                                |
| `pnpm run format:check` | 全绿，exit 0                                                                                                                                               |
| 测试                    | **50 个文件 / 610 例**全通过                                                                                                                               |
| 覆盖率                  | 语句 **96.77%** / 分支 **93.96%** / 函数 **95.49%** / 行 **96.77%**                                                                                        |
| 现存包                  | `config-model`、`form-renderer`、`graph`、`project-format`、`schema-core`、`schema-registry`、`storage`、`test-fixtures`、`ui`、`validator`、`yaml-engine` |
| 现存工具                | `tools/{schema-cli, android-manifest-check, webcrypto-probe}`                                                                                              |
| **本版本从零新建**      | `packages/schema-builtin`、`packages/templates`、`tools/core-test-runner` —— **三个目录全部不存在**（README 把前两个列得像已存在，见「现状核对」）         |

依赖现状：无需引入任何新的运行时依赖。`resolveJsonModule` 已在 `tsconfig.base.json`
开启，Schema Bundle 的 JSON 文件可直接 `import`。React 19 + `@testing-library/react`

- jsdom 已就位（`form-renderer` 与 `apps/web` 在用）。

golden 语料现状：`packages/test-fixtures/fixtures/yaml/` 只有 `comprehensive.yaml`
（3847 字节）、`minimal.yaml`、两份 cycles 样例与一份 adversarial 样例。**退出条件
要求的「官方完整配置样例」不存在**，由 #3 引入。

## 上游事实（本轮实测，2026-08-18）

先决项 C 与内核测试链路都依赖这些值，逐条实测而非引用文档声明：

| 事实                                | 实测值                                                                                                          |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `v1.19.29` tag → commit             | `e26714a181ac0e2fa803453c0a8e9a9ce94e31cb`                                                                      |
| `docs/config.yaml` 是否存在于该 tag | **是**，blob `0598d098e117c6ec3126beaacebc18108a292e45`，134 840 字节                                           |
| Release 是否发布 checksum 清单      | **否** —— v1.19.29 的 assets 里没有任何 SHASUMS / `.sha256` 文件                                                |
| `mihomo-linux-amd64-v1.19.29.gz`    | 17 858 765 字节，GitHub 报告的 digest `sha256:60de76a35a6cbf7b4fa4a20f5c257c24345d1d635ab1aa3877022a1997ef413c` |

第三行是与 ADR-012 的**实质冲突**：ADR-012 落点表写「CI 固定下载 v1.19.29 二进制并
**校验官方 checksum** 后运行配置测试」，但上游根本没有发布 checksum 清单。按既有规则
（以上游实际行为为准，差异记入 `upstream-divergences.md`，不得静默改需求）处理，
落到 #3 的 **D-003** 与 #21 的实现口径。

## 先决项判定

版本文档列三项。逐条核对**代码**而非文档声明。

### 1. `inferControl` 补齐 `oneOf` / `anyOf` 分支 —— 未满足，本版本自解 → #0 / #1

核对 [`packages/schema-core/src/form-plan.ts:253-282`](../../../packages/schema-core/src/form-plan.ts)：
`inferControl` 依次判断 `enum` / `boolean` / `integer` / `number` / `array` / `object` /
`string`，**没有任何 `oneOf` / `anyOf` 分支**，判别式联合走到最后 `return 'unknown'`。
`planField`（同文件 232 行）也只在 `control === 'object'` 时递归规划子字段，联合分支的
属性根本不会被规划。

`ControlType`（[`types.ts:102-116`](../../../packages/schema-core/src/types.ts)）是闭集，
无联合类型控件。按 ADR-002 的两级边界，**新增控件类型属 Level 2（应用代码更新）**——
本版本正是应用发布，可以做，但必须写新 ADR。→ #0（规划侧）+ #1（渲染侧）。

### 2. 扩展 `SchemaModule` 类型 —— 未满足，本版本自解 → #2

核对 [`types.ts:178-182`](../../../packages/schema-core/src/types.ts)：
`SchemaModule = { manifest, schema, ui }`，确无 `validation.rules` / `examples` / `i18n`。
版本文档要求「至少补前三项」，`migrations/*.json` 留 v0.5.0。→ #2。

### 3. 冻结 P0 出站协议字段集合 —— 未满足，需外部信息 → 已决策，#3

仓库内**不存在** v1.19.29 `docs/config.yaml` 的任何副本（全仓库 grep 只在
`config-model` / `apps/web` 的版本号常量里命中 `1.19.29` 字符串）。
`upstream-divergences.md` 待核对项 2 仍是「待核对」。

**已决策（本轮确认）**：把上游 `docs/config.yaml` 按 tag 取回并 **vendor 进仓库**，
连同来源 URL、tag、commit SHA、取回日期一并存档，核对脚本对它做断言。→ #3。

### 附加：内置 Bundle 签名会在 #6 失效 —— 版本文档未列，本版本自解 → #6

[`builtin.ts:37-67`](../../../packages/schema-registry/src/builtin.ts) 的
`BUILTIN_MANIFEST` 携带 `files[].sha256` 与 Ed25519 `signature`，注释明确写着签发用的
bootstrap 私钥「已在签名后立即销毁，从未存储」。#6 起 `BUILTIN_MODULE` 的内容必然改变
→ 文件哈希改变 → manifest 改变 → **签名必然失效**，`verify.test.ts` 会红。

ADR-010 的生产密钥托管（GitHub Environment `schema-release` + `SCHEMA_SIGNING_KEY_B64`

- ≥2 人审批）**尚未建立**：`.github/workflows/` 里没有任何签名 job，`schema-cli` 也没有
  keygen 子命令。但 Bundle 下载与通道是 v0.5.0 范围（版本文档 Out of scope 明列），
  本版本的内置 Bundle 是**编译进应用、从不下载**的离线兜底，其签名是自洽性检查而非供应链
  闸门。

**结论**：沿用 v0.1.0 #8 的既有做法——离线生成一次性 bootstrap 密钥对、重签、内置新公钥、
销毁私钥；`builtin.ts` 的注释原样保留（生产密钥重签仍是 v0.5.0 的义务）。为防止
#7–#11 逐模块改内容时哈希悄悄过期，#6 同时交付一条**再签发回归断言**。不需要你决策。

## 现状核对：文档与代码不符的行

逐条以代码为准。追踪表的 FR/NFR 状态列**无虚标**；不符集中在 README 的目录清单与
若干「留到 v0.3.0」的注释。

| 位置                                          | 现状                                                      | 应改为                              | 修在哪片 |
| --------------------------------------------- | --------------------------------------------------------- | ----------------------------------- | -------- |
| `README.md:32-36`                             | 「M1 已完成…下一步是 M2」                                 | M2 进行中（v0.3.0）                 | #0       |
| `README.md:48`                                | `schema-builtin/` 无状态标记，**目录不存在**              | 标「🚧 规划中」，#6 落地后改 ✅     | #0 → #6  |
| `README.md:52`                                | `migration/` 无状态标记，**目录不存在**                   | 标「🚧 规划中（v0.5.0）」           | #0       |
| `README.md:53`                                | `templates/` 无状态标记，**目录不存在**                   | 标「🚧 规划中」，#20 落地后改 ✅    | #0 → #20 |
| `docs/releases/README.md:14`                  | v0.3.0 状态「未开始」                                     | 进行中                              | #0       |
| `docs/requirements-traceability.md:12`        | 分支 93.97%                                               | 实测 **93.96%**                     | #0       |
| `docs/requirements-traceability.md:14-17`     | 「§8.3 / §8.8 / §8.11 尚未建行」                          | 本版本建行（#6 起逐片补，#22 收口） | #6 → #22 |
| `docs/requirements-traceability.md:25`        | 「M2 及以后 \| 未开始」                                   | M2 进行中（v0.3.0）                 | #0       |
| `packages/validator/src/pipeline.ts:14-16`    | 注释「`security`（v0.3.0）…后续注册」                     | 本版本兑现                          | #13      |
| `packages/validator/src/pipeline.ts:35-44`    | 注释「没有 schema 模块接入流水线，v0.3.0 落地」           | 本版本兑现                          | #12      |
| `packages/schema-registry/src/builtin.ts:5-9` | 注释「真实模块内容 —— 全部 Mihomo 配置段 —— 落在 v0.3.0」 | 本版本兑现                          | #6–#11   |
| `docs/requirements-traceability.md:63`        | FR-PROJ-04 缺口指向「v0.2.0 的 #13」                      | 改指向本计划 #15                    | #15      |
| `docs/requirements-traceability.md:126`       | FR-UPD-01 缺口「真实 Schema 内容留到 v0.3.0」             | 本版本兑现                          | #11      |
| `docs/requirements-traceability.md:149`       | NFR-PERF-03「字段编辑 <100ms 待 v0.3.0 有表单后测」       | 本版本兑现                          | #14      |

## 本轮确认的决策

| 编号 | 决策                                                                                                                                                                                                                                                                                                 |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1   | 上游 `docs/config.yaml` **vendor 进 `packages/test-fixtures/fixtures/upstream/mihomo-v1.19.29/`**（`config.yaml` + `SOURCE.md`），核对脚本对它断言，日后 `upstream-watch` 以它为 diff 基线。落在既有 fixtures 目录下，`.prettierignore` 与 `.gitattributes` 的字节敏感规则**自动生效，无需新增条目** |
| E2   | `tools/core-test-runner` **接入 CI**：下载 v1.19.29 二进制 → 校验固定 digest → 对内置模板跑 `mihomo -t`。因上游无 checksum 清单，digest 取自 GitHub asset digest 并写死（记 D-003）。digest 表从第一天起就是**多架构**结构，本版本只填 linux-amd64 一行                                              |
| E3   | **FR-PROJ-04 纳入本版本**：Worker 协议加 `undo` / `redo` 一对消息，表单编辑走同一历史栈，追踪表转 Done（→ #15）                                                                                                                                                                                      |
| E4   | 判别式联合走**新控件类型 `variant`**：按判别键选分支、把该分支属性规划为 children，非命中分支的文档字段一律保留不删（与基础/高级模式同一条保证）。写 ADR-019                                                                                                                                         |
| E5   | 内置 Bundle 用一次性 bootstrap 密钥**重签**，并交付哈希再签发回归断言；生产密钥托管仍是 v0.5.0 义务（见「先决项附加」）。布局与再签发口径写 ADR-020                                                                                                                                                  |
| E6   | 第二个模板仍按版本文档取「仅代理集合 + 自动选择」，其 `proxy-groups` 段**以未知字段形式原样写入**（`proxy-groups` 模块属 v0.4.0）。模板照样要过 #21 的内核测试——这正好把「未覆盖的段原样保留且内核可用」变成一条真实证据，而不只是 golden 测试里的断言                                               |

## 进度表

| #   | 名称                                                           | 依赖         | 状态 | 提交 SHA  |
| --- | -------------------------------------------------------------- | ------------ | ---- | --------- |
| 0   | 基线校正与 `schema-core` 判别式联合规划（ADR-019）             | —            | Done | `5c5a3e4` |
| 1   | `form-renderer` 协议选择器控件                                 | #0           | Done | `0e2f348` |
| 2   | `SchemaModule` 扩展与模块形状校验                              | —            | Done | `4c3d51e` |
| 3   | vendor 上游 v1.19.29 样例、冻结 P0 字段集合与差异记录          | —            | Done | `4de4b32` |
| 4   | `validation.rules` 受限 DSL 求值器                             | #2           | Done | `5c981f3` |
| 5   | `schema-registry` 转正：发现 / 依赖解析 / 版本选择             | #2           | Done | `f1bc7c5` |
| 6   | `packages/schema-builtin` 建包 + `general` 模块（ADR-020）     | #2、#3、#5   | Done | `1e75aa2` |
| 7   | `dns` 模块                                                     | #6           | Done | `b568b4a` |
| 8   | `sniffer` + `inbound` 模块                                     | #6           | Done | `7f9dedd` |
| 9   | `proxies` 模块 A：联合骨架 + HTTP / SOCKS / SS / Trojan        | #0、#4、#6   | Done | `1679218` |
| 10  | `proxies` 模块 B：VMess / VLESS / Hysteria2 / TUIC / WireGuard | #9           | Done | `c20a78a` |
| 11  | `proxy-providers` 模块                                         | #6           | Done | `a1a13be` |
| 12  | validator `schema` 阶段接入真实模块                            | #5、#6       | Done | `7017a6c` |
| 13  | validator `security` 阶段（FR-VAL-04）                         | #12          | Done | `158305f` |
| 14  | Web 表单页：模块渲染与基础 / 高级模式切换                      | #1、#12      | Done | `3ce816f` |
| 15  | Worker `undo` / `redo` 协议与表单编辑历史栈（FR-PROJ-04）      | #14          | Done | `e6830c4` |
| 16  | 未知字段树与问题中心跳转到表单字段                             | #14          | Done | `7e499bd` |
| 17  | 订阅 URL 管理与本地 Provider 文件预览                          | #11、#14     | Done | `81a5edf` |
| 18  | FR-SCHEMA-04 字段元数据全量断言                                | #10、#11     | Done | `25c53ef` |
| 19  | Golden 五步测试与专项样例                                      | #3、#10、#11 | Done | `0dac513` |
| 20  | `packages/templates` 与两个内置模板                            | #10、#11     | Todo |           |
| 21  | `tools/core-test-runner` 与 CI 内核测试 job                    | #3、#20      | Todo |           |
| 22  | 闭环验收、Alpha 离线产物与文档收口                             | 全部         | Todo |           |

依赖链（按依赖排序，非需求编号顺序）：

- **链 A 控件能力**：#0 → #1
- **链 B 模块契约**：#2 → #4、#2 → #5
- **链 C 上游依据**：#3（独立，可最先做，#6 起全部依赖它）
- **链 D 模块内容**：#6 → {#7、#8、#11}；#6 + #0 + #4 → #9 → #10
- **链 E 校验**：#12 → #13
- **链 F 界面**：#14 → {#15、#16}；#14 + #11 → #17
- **链 G 验收**：#18、#19、#20 → #21 → #22

#0、#2、#3 三片彼此无依赖，是并行入口。#3 单独耗时最长（134 KB 上游样例逐字段核对），
建议最先启动。

## 工程约束（每片实现时都适用）

- Node ≥ 20.11 / pnpm 10。验收 = `pnpm run check` **和** `pnpm run format:check`。
  后者在 CI 是独立步骤，最容易漏。
- 覆盖率阈值（行 / 函数 / 语句 85、分支 80）在 `vitest.config.ts` 强制，**不得放宽**。
  新增包落在既有 `packages/*/src/**` glob 内，自动纳入统计，无需改配置。
- `packages/**` 禁止 `node:fs`（ESLint `no-restricted-imports`）与任何网络调用
  （CI `no-network-egress` job 用 grep 拦 `fetch|XMLHttpRequest|WebSocket|sendBeacon`）。
  **本版本的直接后果**：`schema-builtin` 的 `examples/*.yaml` 不能在运行时读盘，只能由
  测试文件读取（ESLint 对 `**/*.test.ts` 与 `tools/**` 已放开该规则）；`core-test-runner`
  放在 `tools/` 而非 `packages/`，下载与执行二进制在那里才合法。
- 工作区内部包直接导出 TypeScript 源码（ADR-007）：`exports` 指向 `./src/index.ts`。
- 新增包必须同时登记：`packages/<name>/package.json`、`tsconfig.json`、根
  `tsconfig.build.json` 的 `references`，否则 `pnpm run typecheck` 不覆盖它。
- 用户可见文本一律 i18n key，不写字面量（ADR-016）。中英资源文件 key 集合必须相等，
  由 `apps/web/src/i18n/i18n.test.ts` 的双向差集断言强制。**模块自带的 `i18n/*.json`
  同样要有这条断言**（#2 落地）。
- 错误与日志消息不得包含配置值（NFR-SEC-03）。`ValidationIssue` 只携带 i18n key +
  受限参数（路径、字段名、Schema 常量），不携带用户输入的值。新模块产出的每一类问题
  都要有一条「不回显值」的否定用例。
- 跨字段规则 DSL 沿用 `condition.ts` 的封闭算子集合，**不引入正则算子**（NFR-SEC-05）。
- `packages/test-fixtures/fixtures/**` 是字节敏感文件：**只新增，不修改，不格式化**。
  `.prettierignore` 与 `.gitattributes` 已覆盖整个目录，#3 vendor 的上游样例与 #19 的
  golden 专项样例都落在其中，两个文件都不需要改。
- 新增架构决策写新 ADR（本版本预计占用 **ADR-019、ADR-020**），不改已 Accepted 的。
- PRD / ADR 与上游实际行为冲突时以上游为准，记入 `docs/upstream-divergences.md`，
  不得静默改需求。本版本已知一条：**D-003 无官方 checksum 清单**（#3）。
- 分支名 `feat/v0.3.0-<切片名>`，从 `main` 切出，**不在 main 上直接提交**；提交信息用中文。

---

## 切片详情

### #0 基线校正与 `schema-core` 判别式联合规划

**目标**：让 `oneOf` / `anyOf` 判别式联合规划出可渲染的字段树，而不是落进 `unknown`。

**覆盖**：FR-SCHEMA-02（联合类型，规划侧）。

**依赖**：无。

**涉及文件**

- 修改：`packages/schema-core/src/types.ts`（`ControlType` 加 `'variant'`；`PlannedField`
  加联合分支元数据；`UiFieldSpec` 加 `variantLabels?`）
- 修改：`packages/schema-core/src/form-plan.ts`（`inferControl` 加联合分支；`planField`
  在 `control === 'variant'` 时规划命中分支）
- 修改：`packages/schema-core/src/form-plan.test.ts`
- 新建：`docs/adr/ADR-019-discriminated-union-control.md`
- 修改：`docs/adr/README.md`、`README.md`、`docs/releases/README.md`、
  `docs/requirements-traceability.md`（见「现状核对」表中标 #0 的行）

**实现要点**

- **判别键的发现是声明式的，不硬编码 `type`**：分支里凡是 `const` 或单值 `enum` 的属性
  即候选判别键；多个候选时取所有分支都具备的第一个（按属性声明顺序）。九种 P0 出站协议
  按 `type` 判别只是这条通用规则的一个实例——`proxies` 的 Schema 不需要任何特殊照顾，
  这正是 FR-SCHEMA-06 要保住的性质。
- 找不到判别键时**不报错也不猜**：退回 `unknown` 控件（与今天行为一致），并在
  `PlannedField` 上标记原因。合成的坏 Schema 不该让表单崩掉。
- **命中分支的属性规划为 `children`**，路径不变（联合不引入额外路径层级）——
  `proxies[0].password` 的路径必须是 `['proxies', 0, 'password']`，不能是
  `['proxies', 0, 'ss', 'password']`，否则 YAML 回写路径就错了。
- **切换判别值不得删除任何字段**（E4）：规划器是纯函数、不写文档，本身做不到删除；
  要断言的是「文档里存在但当前分支未声明的属性，仍然作为 unknown 字段被规划出来」——
  与基础/高级模式「不丢字段」是同一条保证的另一个入口。这是本片的核心断言。
- `MAX_NESTING`（当前 8）对联合分支同样生效，恶意 Schema 不能靠嵌套联合把规划器打爆。
- ADR-019 记录：为什么新增控件类型而不是复用 `object`（判别键需要独立的选择器语义与
  「切分支不丢字段」的保证）、判别键发现规则、以及这是 ADR-002 的 Level 2 变更。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-02 缺口改为「规划侧已补，渲染侧见 #1」；
ADR 索引新增 ADR-019；README / releases README / 追踪表按「现状核对」表校正。

---

### #1 `form-renderer` 协议选择器控件

**目标**：`variant` 控件渲染为「判别值选择器 + 命中分支字段」，无任何协议专属代码路径。

**覆盖**：FR-SCHEMA-02（渲染侧）、FR-SCHEMA-05 / 06（复验）。

**依赖**：#0。

**涉及文件**

- 修改：`packages/form-renderer/src/controls.tsx`（新增 `variant` 控件）
- 修改：`packages/form-renderer/src/index.tsx`（`variant` 归入 `CONTAINER_CONTROLS`，
  用 `role="group"` + `aria-labelledby`，不挂悬空 `<label>`）
- 修改：`packages/form-renderer/src/index.test.tsx`

**实现要点**

- 判别值选择器是一个普通 `<select>`，选项来自各分支的 `const` / `enum` 值，
  **标签走 i18n key**（`spec.variantLabels` 缺失时回落到判别值本身）。
- **控件里不许出现任何协议名**。用例要直接断言这一点：给一个合成的、与 Mihomo 无关的
  两分支联合（例如 `kind: a | b`），渲染结果同样正确——证明 `proxies` 能工作靠的是
  Schema 而不是渲染器认识「vmess」。
- 切换判别值只 `onChange(判别键路径, 新值)`，**不发出任何删除补丁**。文档里其余字段
  的去留是 YAML 回写层的事，渲染器不做清理——这是 E4 与 PRD §8.3「不能因不在 P0 列表
  中而丢失」的落点。用例断言切换后 `onChange` 只被调用一次、且路径是判别键。
- 非命中分支的字段以 unknown 行呈现（#0 已规划出来），只读、可见、带定位入口。
- 无障碍：判别值变化必须有非颜色的可感知反馈；`aria-` 关系与既有 `object` 控件一致。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-02 → **Done**（指向 `form-plan.test.ts` 的
联合规划用例与 `index.test.tsx` 的合成两分支用例）。

---

### #2 `SchemaModule` 扩展与模块形状校验

**目标**：把 `SchemaModule` 从 `{ manifest, schema, ui }` 扩到 PRD §9.3 的模块结构，
并给出一个可复用的形状校验器。

**覆盖**：先决项 2；FR-SCHEMA-03 / 04 的承载结构。

**依赖**：无。

**涉及文件**

- 修改：`packages/schema-core/src/types.ts`（`SchemaModule` 加 `rules?`、`examples?`、
  `i18n?`；新增 `ValidationRule` / `ModuleExample` / `ModuleI18n` 类型）
- 新建：`packages/schema-core/src/module.ts`、`src/module.test.ts`（形状校验）
- 修改：`packages/schema-core/src/index.ts`

**实现要点**

- **`migrations` 本版本不加**。版本文档只要求前三项，加一个没有消费者的字段只会诱导
  别人往里塞东西。v0.5.0 迁移能力落地时再加，那时它才有语义。
- `ValidationRule` 的形状：`{ id, severity, when: Condition, messageKey, messageParams?, path?, fix? }`。
  `when` **复用既有 `Condition` 类型**，不新建 DSL——ADR-002 要求 Bundle 不能引入新操作码，
  复用是唯一符合该约束的做法。求值器在 #4。
- `ModuleI18n` 是 `Record<Locale, Record<string, string>>`。形状校验强制**每种语言的 key
  集合完全相等**（双向差集），与 `apps/web/src/i18n/i18n.test.ts` 同一条口径，
  且值不得为空字符串。
- `ModuleExample`：`{ name, kind: 'valid' | 'invalid' | 'edge' | 'unknown-fields', path }`。
  `path` 指向包内的 `examples/*.yaml`，**类型里只放路径不放内容**——内容读盘是测试的事
  （`packages/**` 禁 `node:fs`）。退出条件「每个 P0 模块都有合法、非法、边界和未知字段
  测试样例」由这四个 `kind` 直接对应，#22 可以机器核对齐全性。
- `validateModuleShape(module)` 返回 `ValidationIssue` 形状的问题列表而非抛异常，
  并且**消息不含任何模块内容**（NFR-SEC-03 的同一条底线，要有否定用例）。
- 向后兼容：三个新字段都是可选的，`BUILTIN_MODULE` 与 `sample-module.ts` 不改也应编译通过。
  这条要有用例，否则 #6 之前的所有切片都会被迫连带改动。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-03 / 04 缺口更新为「承载结构已就位，
求值器见 #4、断言见 #18」。

---

### #3 vendor 上游 v1.19.29 样例、冻结 P0 字段集合与差异记录

**目标**：把权威比对对象放进仓库，冻结六个 P0 模块的字段集合，并把「核对」变成可回归的
断言而不是一次性人工阅读。

**覆盖**：先决项 3；ADR-012 派生任务；退出条件 2 的基线。

**依赖**：无。**建议最先启动**——#6 起全部模块切片都依赖它，且它自身耗时最长。

**涉及文件**

- 新建：`packages/test-fixtures/fixtures/upstream/mihomo-v1.19.29/config.yaml`
  （vendor 原文，134 840 字节，不改一个字节）
- 新建：`packages/test-fixtures/fixtures/upstream/mihomo-v1.19.29/SOURCE.md`
  （来源 URL、tag、commit SHA、blob SHA、取回日期、上游许可证声明）
- 新建：`packages/test-fixtures/src/upstream.ts`、`src/upstream.test.ts`（字段集合常量 + 核对断言）
- 修改：`docs/upstream-divergences.md`（关闭待核对项 2；新增 D-003）
- **无需改** `.prettierignore` / `.gitattributes`：两者已有
  `packages/test-fixtures/fixtures/**` 的字节敏感规则，新目录落在其覆盖范围内

**实现要点**

- **原文按字节 vendor，不做任何规范化**。取回参数写死在 `SOURCE.md`：
  仓库 `MetaCubeX/mihomo`，tag `v1.19.29`，commit `e26714a181ac0e2fa803453c0a8e9a9ce94e31cb`，
  blob `0598d098e117c6ec3126beaacebc18108a292e45`，134 840 字节。
  `upstream.test.ts` 断言文件的 SHA-256 与记录值一致——**任何人「顺手整理」这份文件
  都会立刻红**，这是它作为权威依据能成立的前提。
- 冻结产物是一份**机器可读的字段清单**（`UPSTREAM_P0_FIELDS`），而不是一段散文：
  `Record<模块 id, { path: string; presentUpstream: boolean; note?: string }[]>`，
  六个模块 + 九种出站协议逐字段列出。#6–#11 的每个模块切片都对它做双向断言
  （Schema 声明了但上游没有 → 红；上游有且属 P0 范围但 Schema 没声明 → 红）。
- **P0 边界严格按 PRD §8.3 的表**切：出站节点 P0 = HTTP / SOCKS / SS / VMess / VLESS /
  Trojan / Hysteria2 / TUIC / WireGuard，其余（AnyTLS、Mieru、MASQUE、TrustTunnel、
  OpenVPN 等）**一律不进 Schema**，走未知字段树。清单里给它们留 `presentUpstream: true`
  - `note: 'P1/P2'` 的行，明确「知道它存在且故意不覆盖」，与「漏了」区分开。
- **D-003 必写**：ADR-012 落点表要求「校验官方 checksum」，但 v1.19.29 的 release
  没有发布任何 SHASUMS / `.sha256` 资产（本轮实测）。处理：改为固定 GitHub 报告的 asset
  digest，值与来源写进 #21 的 runner 并加注释。**不改 ADR-012 原文**（已 Accepted），
  差异记在 `upstream-divergences.md`。
- 核对过程中发现的其它 PRD ↔ 上游冲突，逐条追加 D-xxx，**不得静默调整 Schema 去迁就 PRD**。
- vendor 的是上游的示例配置文档，`SOURCE.md` 要写明其许可证与「本仓库只作为比对基线引用」。
- 放在 `fixtures/` 下的副作用要认掉：`vitest.config.ts` 的 `coverage.exclude` 已包含
  `packages/test-fixtures/**`，新增的 `src/upstream.ts` **不进覆盖率统计**。这是既有
  口径（`test-fixtures` 整包都不计），不是本片放宽阈值——但它意味着字段清单的正确性
  全靠 `upstream.test.ts` 的断言，不靠覆盖率兜底，断言要写足。

**验收命令**

```bash
pnpm run check && pnpm run format:check && git check-attr -a packages/test-fixtures/fixtures/upstream/mihomo-v1.19.29/config.yaml
```

**完成后更新的文档行**：`upstream-divergences.md` 待核对项 2 → 已关闭；新增 D-003；
追踪表新建 §8.3 小节（六个模块行，全部 Todo，证据列指向本片的字段清单）。

---

### #4 `validation.rules` 受限 DSL 求值器

**目标**：让跨字段、跨对象的互斥与依赖约束能由 Bundle 声明，而不必改应用代码。

**覆盖**：FR-SCHEMA-03（互斥与依赖字段）。

**依赖**：#2。

**涉及文件**

- 新建：`packages/schema-core/src/rules.ts`、`src/rules.test.ts`
- 修改：`packages/schema-core/src/index.ts`

**实现要点**

- **一个新算子都不加**（NFR-SEC-05 / ADR-002）。互斥用 `{ op: 'not', of: { op: 'and',
of: [{ op: 'exists', path: 'a' }, { op: 'exists', path: 'b' }] } }` 表达；依赖用
  `{ op: 'or', of: [{ op: 'not', of: {...exists a} }, {...exists b} ] }`。求值器只是把
  已有的 `evaluateCondition` 应用到规则列表上并产出 `ValidationIssue`。
- **规则的作用域是模块根**，`$.` 前缀寻址已由 `condition.ts` 支持；跨对象（例如
  `proxies[]` 的每一项）用 `path` 上的通配段展开，展开逻辑在本片实现且**上限固定**
  （匹配项数量有上限，避免 1 MB 配置 × N 条规则的组合爆炸）。
- 产出的问题**只带路径与规则 id，不带命中的值**（NFR-SEC-03），要有用真实密钥值驱动的
  否定用例。
- `severity` 由规则自己声明；`blocking` 仍由 `@mcs/validator` 的既有口径统一判定，
  本片不重新发明规则。
- **记录 DSL 表达不了的约束**：核对上游时若发现某条约束（例如「`port` 与 `ports`
  互斥且至少其一」的复杂变体）无法用封闭算子集表达，写进 ADR-019 的「DSL 演进输入」
  小节或新建记录，**不要偷偷升级成应用代码里的 if**。ADR-002 已接受这个代价，
  但代价必须可见。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-03 → **Done**（指向 `rules.test.ts` 的互斥
与依赖用例 + `condition.test.ts`）。

---

### #5 `schema-registry` 转正：发现 / 依赖解析 / 版本选择

**目标**：模块能被注册和解析出来，页面不需要认识任何模块 id。

**覆盖**：FR-SCHEMA-05。

**依赖**：#2。

**涉及文件**

- 新建：`packages/schema-registry/src/registry.ts`、`src/registry.test.ts`
- 修改：`packages/schema-registry/src/index.ts`

**实现要点**

- `createRegistry(bundle)` → `{ modules(), byRoot(path), resolve(id) }`。**发现的输入是
  `StoredBundle`**（已经过 `verifyBundle` 的产物），不是文件系统扫描——`packages/**`
  禁 `node:fs`，而且「未经验签的模块不得进入 Registry」本身就是安全边界。
- `ModuleManifest.dependsOn` 今天全仓库只在类型里出现，无任何消费者。本片给它真实语义：
  拓扑排序，**环路必须报错而不是无限循环**（复用 `packages/graph` 的检测思路，但不引入
  跨包依赖——模块图规模很小，本地实现十几行）。
- 版本选择：同一 `id` 出现多份时按 SemVer 取最高且满足 `mihomo.minVersion` ≤ 项目
  兼容档案 ≤ `maxTestedVersion` 的那份；**无满足项时明确失败，不静默回落**
  （静默回落会让用户以为字段被支持）。
- **「不依赖中央页面修改」要有结构性断言**，与 v0.2.0 的 Worker 边界断言同一手法：
  一条静态用例扫描 `apps/web/src`，断言其中不出现任何硬编码的模块 id 字面量
  （`'general'` / `'dns'` / `'proxies'` …）——页面只能经 Registry 拿模块列表。
  这条把 FR-SCHEMA-05 从「设计意图」变成可自动化的事实。
- 解析失败（缺依赖、环路、无匹配版本）产出 `ValidationIssue`，消息不含模块内容。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-05 → **Done**（指向 `registry.test.ts` 与
`apps/web` 的无硬编码模块 id 静态断言）。

---

### #6 `packages/schema-builtin` 建包 + `general` 模块

**目标**：建立内置 Bundle 的物理布局与再签发机制，并交付第一个真实模块。

**覆盖**：FR-UPD-01（真实内容）；PRD §8.3 全局配置 P0；退出条件 2 / 4 的第一片。

**依赖**：#2、#3、#5。**本片定死后续五个模块的模板**，先做对再复制。

**涉及文件**

- 新建：`packages/schema-builtin/package.json`、`tsconfig.json`、`src/index.ts`、
  `src/builtin.test.ts`
- 新建：`packages/schema-builtin/modules/general/{module.manifest.json, config.schema.json,
ui.schema.json, validation.rules.json, i18n/zh-CN.json, i18n/en.json, examples/*.yaml}`
- 新建：`docs/adr/ADR-020-builtin-bundle-layout-and-reissue.md`
- 修改：`packages/schema-registry/src/builtin.ts`（`BUILTIN_*` 改为从 `@mcs/schema-builtin` 取，
  manifest 重算重签）
- 修改：根 `tsconfig.build.json`、`docs/adr/README.md`、`README.md`

**实现要点**

- **模块以 JSON 文件形式存在于磁盘上**，不是 TS 字面量。理由：这些文件就是
  v0.5.0 要被 `schema-cli pack` 打包、逐文件 SHA-256、签名下发的那些文件；写成 TS
  就得在发布时再转一道，转换本身会成为「Bundle 只是数据」的漏洞。`resolveJsonModule`
  已开启，`src/index.ts` 直接 `import`。`examples/*.yaml` 只由测试读取（`packages/**` 禁 fs）。
- **再签发机制（E5）**：`builtin.test.ts` 逐文件重算 SHA-256 并与 `BUILTIN_MANIFEST.files[]`
  比对，不一致即红；另有一条用例跑完整 `verifyBundle`。#7–#11 每次改模块内容都必须
  重新签发，这条断言保证「忘了重签」不会溜过去。签发用一次性 bootstrap 密钥对，
  私钥当场销毁，`builtin.ts` 现有注释（生产密钥重签是 v0.5.0 义务）**原样保留**。
- `general` 模块字段按 PRD §8.3 P0 列：`mode`、`log-level`、IPv6、LAN（`allow-lan` /
  `bind-address` / `lan-allowed-ips` 等）、controller（`external-controller` / `secret`）、
  profile、GEO、连接选项。逐字段对 #3 的 `UPSTREAM_P0_FIELDS` 双向断言。
- **敏感字段就地复验 NFR-SEC-02**：`secret`（Controller Secret）必须被 `inferControl`
  判为 `secret` 控件——这是版本文档要求「在真实字段上复验」的第一处。
- 四类 `examples/*.yaml` 齐备（valid / invalid / edge / unknown-fields），退出条件 4
  从本片起逐模块累积。
- ADR-020 记录：磁盘 JSON 布局、`src/index.ts` 的导入口径、再签发规则与 bootstrap 密钥
  的定位（为什么它不是 ADR-010 的生产密钥）。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表新增 §8.3「全局配置」行 → Done；FR-UPD-01 缺口更新为
「general 已落地，其余模块见 #7–#11」；ADR 索引新增 ADR-020；README `schema-builtin/` 行改 ✅。

---

### #7 `dns` 模块

**目标**：交付 DNS 模块的 P0 字段集合。

**覆盖**：PRD §8.3 DNS P0；FR-SCHEMA-01 / 06 在真实模块上复验。

**依赖**：#6。

**涉及文件**

- 新建：`packages/schema-builtin/modules/dns/**`（与 #6 同一套七类文件）
- 修改：`packages/schema-builtin/src/index.ts`、`packages/schema-registry/src/builtin.ts`（重签）
- 修改（执行时修正，见下）：`packages/schema-builtin/modules/general/{config.schema.json,ui.schema.json,i18n/*.json}`、
  `packages/test-fixtures/src/upstream.ts`、`packages/schema-builtin/src/builtin.test.ts`

**实现要点**

- 字段范围：`enable`、`listen`、`enhanced-mode`、`fake-ip-range` / `fake-ip-filter`、
  `nameserver`、`fallback`（含 `fallback-filter`）、`nameserver-policy`、`hosts`。
- `nameserver-policy` 与 `hosts` 是自由映射，应落到 `key-value` 控件——正好复验
  `inferControl` 对「无声明属性的 object」的判定在真实字段上成立。
- `fallback-filter` 的 `geoip` / `ipcidr` / `domain` 的互斥与依赖关系用 #4 的
  `validation.rules.json` 表达，不写进应用代码。DSL 表达不了的记录下来（见 #4）。
- 逐字段对 `UPSTREAM_P0_FIELDS` 双向断言；四类 examples 齐备。

**执行时修正**

- **`hosts` 改落到 `general`，不落在 `dns`**：`hosts` 在上游样例里是文档根键
  （不在 `dns:` 缩进块下），而本片给 `dns` 模块定的 `root: ['dns']` 只能触达
  `dns:` 自己的子树——把 `hosts` 声明在 `dns` 的 Schema 里会让它在真实文档里
  永远读不到值。只有文档根模块（`general`，`root: []`）能声明它，PRD 把它归在
  DNS 概念下只是主题分组，不是文档位置。已把 `hosts` 从 `UPSTREAM_P0_FIELDS`
  的 `DNS_FIELDS` 移到 `GENERAL_FIELDS`，并给 `general` 的
  `config.schema.json` / `ui.schema.json` / 两份 `i18n` 补上该字段（连带触发
  一次 Bundle 重签，`general.json` 内容哈希随之变化）。
- **补上 `fallback-filter` 及其四个子字段**：#3 冻结 `UPSTREAM_P0_FIELDS` 时的
  清单遗漏了 `fallback-filter`（本片开工前发现，不是本片引入的缺口）。已按上游
  样例补齐 `geoip` / `geoip-code` / `ipcidr` / `domain`；`geosite` 子字段因上游
  文档明确标注弃用（改用 `nameserver-policy`）而不纳入。
- **两处测试基建修正**（均被本片新写的测试当场捕获，不是缺陷遗留）：
  1. `builtin.test.ts` 比对 `UPSTREAM_P0_FIELDS` 时，此前直接用
     `collectSchemaPaths()` 的裸路径（相对模块自身 `root`）去比文档路径——
     `general` 的 `root: []` 让两种口径巧合一致，这个 gap 一直没暴露，直到
     `dns` 的 `root: ['dns']` 才第一次被检出。现在按 `manifest.root` 前缀把
     裸路径还原成文档路径再比较。
  2. `buildFormPlan(module, documentValue, ...)` 的约定是「传整份文档，内部按
     `manifest.root` 自己取子树」（`SchemaFormProps.value` 的文档注释），而
     example 文件本身是按模块自身根定稿的（与 `validateValue` 的调用约定一
     致）。测试里补一个 `toDocument()` 帮手按 `root` 包一层再传给
     `buildFormPlan`，example 文件不改。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 §8.3「DNS」行 → Done；`general` 行补充 `hosts`。

---

### #8 `sniffer` + `inbound` 模块

**目标**：交付域名嗅探与入站两个模块的 P0 字段集合。

**覆盖**：PRD §8.3 域名嗅探 / 入站 P0。

**依赖**：#6。

**涉及文件**

- 新建：`packages/schema-builtin/modules/sniffer/**`、`modules/inbound/**`
- 修改：`packages/schema-builtin/src/index.ts`、`packages/schema-registry/src/builtin.ts`（重签）

**实现要点**

- 两个模块合并一片是因为二者都小：`sniffer` P0 只有常用嗅探开关与协议
  （`sniff` 下的 HTTP / TLS / QUIC 分组，`force-domain` / `skip-domain` 的高级
  force/skip 条件属 P1，不覆盖）；`inbound` P0 只有 HTTP / SOCKS / Mixed 端口与常用 TUN。
- **`inbound` 的 TUN 字段必须标平台**（`UiFieldSpec.platforms`），复验既有的
  「非目标平台字段隐藏但不删除」用例在真实字段上成立。
- **`allow-lan` 相关字段带 `safety: 'caution'`**，为 #13 的 FR-VAL-04 安全警告留出元数据
  依据——警告的判定放在 validator，标记放在 Schema，两侧不重复表达。
- 全部 Listeners 与服务端协议属 P1，走未知字段树，不进 Schema。
- 逐字段双向断言；两个模块各自四类 examples 齐备。

**执行时修正**

- **跨模块"未知字段"去重问题不在本片解决，明确改留给 #14**：`inbound` 的
  `root: []` 与 `general` 相同，本片是这个共享关系第一次真正落地（#6 的
  `general` 行早先写"留给 #8 解决"，但那时候还只是预判）。落地后能看清的
  事实是——`buildFormPlan` 一次只处理一个模块，`root: []` 时 `scope` 等于整份
  文档，`general`/`inbound` 各自的"未知字段"循环会把对方**已经声明**的字段
  错认成未知。但本片两个模块的测试都只喂各自独立的合成 example（不含对方的
  字段），这个问题不会让任何测试变红，纯粹是潜在的、要等 #14 真的把两个模块
  的表单渲染到同一份文档上才会暴露。现在就设计修复机制（比如给
  `buildFormPlan` 加一个 `ignoreUnknownKeys` 选项）只能是没有真实调用方的猜测
  ——#14 到底是"每个模块一个独立表单"还是"同根模块合并渲染成一个表单"决定了
  正确的修复形状，而这个 UI 设计现在还没定。`packages/test-fixtures/src/upstream.ts`
  的 `general` 字段注释、`docs/requirements-traceability.md` 的 general/inbound
  两行、本文件 #14 小节都记了这笔账，留一条线索而不是让问题在下一次触及时
  失去上下文。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 §8.3「域名嗅探」「入站」两行 → Done。

---

### #9 `proxies` 模块 A：联合骨架 + HTTP / SOCKS / SS / Trojan

**目标**：把九种出站协议的判别式联合骨架搭起来，先交付四种结构较简单的协议。

**覆盖**：PRD §8.3 出站节点 P0（前四种）；FR-SCHEMA-02 在真实字段上复验。

**依赖**：#0、#4、#6。**本版本的核心风险片**——版本文档「本版本要回答的问题」
就在这里得到答案。

**涉及文件**

- 新建：`packages/schema-builtin/modules/proxies/**`
- 修改：`packages/schema-builtin/src/index.ts`、`packages/schema-registry/src/builtin.ts`（重签）

**实现要点**

- `proxies` 是数组，元素是按 `type` 判别的 `oneOf`。**共享字段（`name`、`server`、
  `port`、`udp` 等）放 `$defs` 并由各分支 `allOf` 引入**——`$ref` 只允许本地
  `#/$defs/*`（ADR-008），这条不能破。
- **本片的验收不是「四种协议能填」，而是「加第五种协议不需要改应用代码」**。
  用例要显式证明：#10 新增的五种协议只往 `config.schema.json` / `ui.schema.json` 加分支，
  `packages/form-renderer` 与 `apps/web` 一行不改。为此本片就要有一条「向联合追加一个
  合成分支，渲染即可用」的用例（与 M0 的 FR-SCHEMA-06 用例同一手法，但在真实模块上）。
- 敏感字段复验 NFR-SEC-02：`password`（SS / Trojan）必须默认遮罩。
- 若判别式联合确实表达不出某种协议的差异，**立刻停下来记录**并按版本文档风险表的缓解
  路径处理（为协议选择器单独设计控件类型已在 #0/#1 做完；若仍不够，说明需要 Level 2
  的进一步扩展，必须写进 ADR-019 的补充而不是在渲染器里加 if）。
- 逐字段对 `UPSTREAM_P0_FIELDS` 双向断言；四类 examples 齐备。

**执行时修正**

- **本片核心风险的答案是"是，但有一个具体的、已记录的边界"**：判别式联合机制
  （#0/#1 在合成样例上证明）在真实、混乱的 Mihomo 协议字段上依然成立——`password`
  在 http/socks5/ss/trojan 四种协议上都被自动判定为 `secret` 控件（NFR-SEC-02，
  无需显式 UI 覆盖）；新增第五个分支（合成 `vmess` + `uuid`）不需要改
  `packages/form-renderer`/`apps/web` 任何一行，`packages/schema-core` 也不用改
  ——同一份探测 harness 立刻能用。这不是"判别式联合表达不出协议差异"（第 662 行的
  停下条件），是另一个更窄的边界：
- **`config.schema.json` 描述的是数组的单个元素，不是 `proxies:` 整个数组**：
  `buildFormPlan` 只在具名字段上识别 `oneOf`/`anyOf`（`planField` 的职责），从不
  识别它正在规划的 schema 自己根层级的联合（`planObject` 只看 `.properties`）。
  一个 `proxies[]` 元素本身没有包装键（就是 `{name, type, server, ...}`），在
  schema 里为了迁就今天的规划器硬套一个假包装键，会让这个模块的存储内容不再如实
  对应磁盘上真实文档的样子——这是每个模块目前为止都成立的不变量，不该为这一个
  模块破例。所以：存储的 schema 保持诚实，`builtin.test.ts` 自己用一次性包装
  harness（`planOneProxy`，与 `form-plan.test.ts` 的 `variantModule` 同一手法，
  只是指向 `PROXIES_MODULE` 真实的 `$defs`/`oneOf` 而非临时构造的）驱动
  `buildFormPlan` 来完成这条证明，包装只在测试里发生，从不进入存储模块。
- **由此推出的、真正留到 #14 的缺口**：`buildFormPlan(PROXIES_MODULE, 整份真实
文档)` 目前还不能端到端直接调用——数组不是 record，`planObject` 会把它当成
  `{}`，一个字段都规划不出来。把"一个元素的 plan"变成"N 个渲染表单、每个的路径
  都带 `proxies.<i>.` 前缀"，需要遍历数组的那段代码来做包装与拆分——这正是 #14
  要做的事，处理原则与 general/inbound 共享文档根的缺口相同（同一小节记录）。
  `manifest.root: ['proxies']` 保留：它仍然如实记录这个模块拥有文档里的哪条路径，
  只是"路径"与"能直接喂给 `buildFormPlan`"这两件事，对 `proxies` 这一个模块来说
  第一次分开了。
- 通用的 `describe.each(MODULES)` 表格测试对 `proxies` 不适用（`collectSchemaPaths`
  只走 `.properties`，看不到 `$defs`/`oneOf`），改写了专用的
  `describe('proxies module', ...)` 块，含专用的 `collectProxyFieldPaths`（读
  `$defs` 还原 `_shared.<key>`/`<protocol>.<key>` 路径，与 `UPSTREAM_P0_FIELDS.proxies`
  的既有命名对齐）。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 §8.3「出站节点」行 → Partial（四种已覆盖，其余见 #10）。

---

### #10 `proxies` 模块 B：VMess / VLESS / Hysteria2 / TUIC / WireGuard

**目标**：补齐其余五种 P0 出站协议。

**覆盖**：PRD §8.3 出站节点 P0（后五种）；FR-SCHEMA-06 的实证。

**依赖**：#9。

**涉及文件**

- 修改：`packages/schema-builtin/modules/proxies/{config.schema.json, ui.schema.json,
validation.rules.json, i18n/*.json}`、新增对应 `examples/*.yaml`
- 修改：`packages/schema-registry/src/builtin.ts`（重签）

**实现要点**

- **本片是 ADR-002 成立与否的实证**：五种协议全部只改 Schema / UI Schema / rules / i18n
  这四类**数据文件**，`packages/form-renderer`、`apps/web`、`packages/schema-core`
  一行代码都不改。若发现必须改代码，那就是 Level 2 边界的真实位置——**如实记录，
  不要为了让结论好看而绕开**。提交信息里要写明本片是否做到了零代码改动。
- 敏感字段复验 NFR-SEC-02 的其余两类：`uuid`（VMess / VLESS / TUIC）、
  `private-key`（WireGuard）——版本文档点名的三类敏感字段至此在真实字段上全部复验。
- WireGuard 的多行 key 与 `reserved` 数组是 golden 测试「多行密钥」专项样例的来源，
  样例文件在本片产出，#19 消费。
- 逐字段双向断言；五种协议各自的 examples 齐备。

**执行时修正**

- **零代码改动确认成立**：五种新协议只改了 `schema-builtin/modules/proxies/` 下的
  四类数据文件，`packages/form-renderer`、`apps/web`、`packages/schema-core` 一行
  未改——ADR-002 的核心声明在真实协议字段上得到复验。测试里额外加了一条"加第十个
  分支"（超出完整九协议集一步，合成 `ssr`）的用例，同一探测 harness 立即可用，
  进一步确认 #9 证明的机制不是恰好对四种协议凑巧成立。
- **发现一处真实的共享字段缺陷，已修正（纯数据改动，不影响零代码改动的结论）**：
  `sni`/`skip-cert-verify` 在 #9 时共享同一条 `visibleWhen: tls===true`（http/
  socks5/trojan 都恰好有 `tls` 兄弟字段，掩盖了问题）。hysteria2/tuic 没有 `tls`
  字段（协议本身基于 QUIC，隐含 TLS），这条条件会让这两个协议的 `sni`/
  `skip-cert-verify` 被错误地永久隐藏。按名称共享 UI 字段本身是既定设计（减少
  `ui.schema.json` 的重复），但可见性条件必须对所有共享该名称的协议都成立——已
  去掉该条件，两个字段现在对所有声明它们的协议都始终可见。
- `evaluateRules` 复验：TUIC 的 `token`（v4 旧版鉴权）与 `uuid`+`password`（v5）
  互斥是上游文档明确写出的约束（"不可同时填写"），补了一条
  `validation.rules.json` 规则并双向验证。
- WireGuard 的多行 `private-key`（YAML 块标量）+ `reserved` 数组形式落在
  `examples/edge.yaml`；#19 直接复用其内容作为 golden「多行密钥」专项样例的来源。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 §8.3「出站节点」行 → Done；FR-SCHEMA-06 证据列补本片
的零代码改动记录。

---

### #11 `proxy-providers` 模块

**目标**：交付代理集合模块的 P0 字段集合。

**覆盖**：PRD §8.3 代理集合 P0；FR-UPD-01 收口。

**依赖**：#6。

**涉及文件**

- 新建：`packages/schema-builtin/modules/proxy-providers/**`
- 修改：`packages/schema-builtin/src/index.ts`、`packages/schema-registry/src/builtin.ts`（重签）

**实现要点**

- 字段范围：`type`（HTTP / File / Inline，又一处判别式联合）、`url`、`path`、`interval`、
  `health-check`（`enable` / `url` / `interval` / `lazy`）、`filter` / `exclude-filter`、
  `override`。加密内容与复杂 header 属 P1，不覆盖。
- **`url` 字段是订阅地址，必须 `sensitive: true`**。注意它与 `health-check.url`（探测
  地址，非敏感）**同名不同义**——`project-format` 的 `describeSensitivity` 因为只做正则
  扫描而对两者都报（v0.2.0 已如实记录该取舍）；本片在 Schema 层可以精确区分，
  两者各自标注，不要跟着启发式一起模糊掉。
- `filter` / `exclude-filter` 的值是**用户提供的正则**。NFR-SEC-05 禁止的是把攻击者
  正则**交给校验器执行**；这里正则是配置内容、由 Mihomo 执行，我们只做长度上限与
  `isRiskyPattern()` 形状警告（既有能力），**绝不在校验流程里 `new RegExp` 它去匹配**。
  这条要有注释和用例，否则很容易被后人「顺手加个预览功能」破坏。
- 逐字段双向断言；四类 examples 齐备。

**执行时修正**

- **D-004 收口**：`filter`/`exclude-filter` 是否为 `proxy-providers` 自身字段，
  vendor 样例本身没能确认（第 3 片已如实标 `presentUpstream: false`）。本片按
  D-004 的要求另行核对——官方 Meta-Docs 源码
  （`github.com/MetaCubeX/Meta-Docs`，`docs/config/proxy-providers/index.md`）
  明确把 `filter`、`exclude-filter`、`exclude-type` 列为顶层字段，并给出示例
  （`filter: "(?i)港|hk|hongkong|hong kong"`）。`UPSTREAM_P0_FIELDS['proxy-providers']`
  已按此更新，`docs/upstream-divergences.md` 的 D-004 一并收口；`exclude-type`
  记录但不建模（不在本片显式字段范围内）。
- **发现并修正一处 #10 遗留的潜在遮罩缺陷**：设计 `url` 的 `sensitive: true` 时
  重新读了一遍 `form-renderer` 的渲染路径，发现 `UiFieldSpec.sensitive` 的文档
  注释承诺"默认遮罩"，但实际实现里 `sensitive` 只驱动 `data-sensitive` DOM
  属性，真正选择 `SecretControl` 渲染的是 `control` 字段——`sensitive: true`
  单独设置并不会遮罩。这条路径此前从未被验证过，因为 #6–#10 每一个敏感字段
  （`secret`/`password`/`uuid`/`private-key`/`token`）都恰好命中
  `SENSITIVE_KEY` 正则、被 `inferControl` 自动识别为 `secret` 控件，从未依赖过
  `sensitive` 单独生效。#10 的 `obfs-password`（不匹配正则，只设了
  `sensitive: true`）是第一个真正暴露这条缺口的字段。修法：`url` 与
  `obfs-password` 都补上显式 `control: 'secret'`（纯 UI 数据修正，不改
  `schema-core`/`form-renderer` 代码）。`sensitive` 该不该独立驱动控件选择，
  留给未来一次 `schema-core` 打磨，不在本片处理。
- `collectProxyFieldPaths`/`planOneProxy`（#9 引入）泛化重命名为
  `collectUnionFieldPaths`/`planOneUnionItem`，接受任意判别式联合模块，
  `proxies`/`proxy-providers` 两个模块的测试共用同一套 harness。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 §8.3「代理集合」行 → Done；FR-UPD-01 → **Done**
（六模块齐备，缺口清空）；退出条件 4 的六个模块样例至此全部到位。

---

### #12 validator `schema` 阶段接入真实模块

**目标**：让 `schemaStage` 真正跑起来，把 `ValidationIssue` 的内容填满。

**覆盖**：FR-VAL-01（填内容）、FR-VAL-05（接入问题中心）。

**依赖**：#5、#6。

**涉及文件**

- 修改：`packages/validator/src/pipeline.ts`（`PipelineContext` 加模块解析结果；
  `schemaStage` 改为真实实现）
- 修改：`packages/validator/src/pipeline.test.ts`
- 修改：`packages/validator/package.json` / `tsconfig.json`（加 `@mcs/schema-registry` 依赖与 reference）

**实现要点**

- 阶段接口**不改形状**——v0.2.0 已把 `ValidationStage { id, run(ctx) }` 定死，本片只往
  `PipelineContext` 加字段。若发现必须改接口，那是 v0.2.0 的设计失误，要如实记录。
- `schemaStage` = 对每个已解析模块跑 `validateSchema(module.schema, 文档子树)` +
  跑 #4 的规则求值器，产出的 `path` 由既有适配器经 `document.locate(path)` 补 `range`。
- **FR-VAL-05 是硬约束**：Schema 未覆盖的字段标记 `severity: 'info'` / `code: 'unknown-field'`，
  **绝不判 Error、绝不阻断导出**。这条要有用例：一份带大量 P1/P2 协议字段的配置
  必须 `hasBlockingIssues() === false`。
- 语法阶段短路逻辑不变；本片新增「schema 阶段产出的 error 也阻断导出」的用例。
- 消息不含配置值的否定用例扩展到新模块（版本文档 §7 明确要求）。

**执行时修正**

- **文件范围比计划列的多一个：`packages/validator/src/issue.ts`（连带 `index.ts` 的
  导出列表）**。`rules.ts` 里 `RuleIssue` 的类型注释早就写明"a future adapter
  (v0.3.0 #12) widens it the way `fromSchemaIssue` does"——`schemaStage` 既要跑
  `evaluateRules`，就必须有 `fromRuleIssue` 把 `RuleIssue` 收敛进
  `ValidationIssue`，这条适配器只能加在 `fromSchemaIssue`/`fromYamlIssue` 已经在
  的地方。顺带给 `KERNEL_MODULES` 添了 `'schema'`——`unknown-field` 这类"没有任何
  已装模块认领"的结论，产出者是 schema 阶段本身，不是某个具体 Schema 模块，此前
  这个 producer 身份没处安放。
- **FR-VAL-05 的"未覆盖字段"判定用 `document.leafPaths()` 而不是逐模块跑
  `buildFormPlan().unknownFields`**：后者会把 general/inbound 共享文档根
  （`root: []`，v0.3.0 #8 的遗留缺口）直接当真——每个模块独立跑一遍，general 会把
  inbound 的字段全标"未知"，反之亦然，两边全错。改成"每个已装模块各自跑一遍
  `buildFormPlan`，把它们各自认领的路径取并集，再用 `document.leafPaths()`
  （文档里每一条真实存在的叶子路径，与模块无关）减去这个并集"——一个字段只要被
  *任一*已装模块认领就不算未知。这顺带把 #8/#9 留给 #14 的缺口在**校验层**解决
  了（`buildFormPlan` 本身在**渲染层**的同一缺口仍然存在，#14 仍要处理，两者是
  不同的消费者、不同的修法）。
- **模块自身整段在文档里完全缺失时（`document.getIn(module.manifest.root)` 返回
  `undefined`，或字面 `dns:` 空值返回 `null`），跳过该模块的 `validateValue`/
  `evaluateRules`，不当成一次 `schema.type` 违规**：六个模块的顶层 schema 都没有
  声明 `required`（允许整段省略），但 `validate.ts` 的 `walk()` 本身对
  `undefined`/`null` 没有特判——一个用户完全没写 `dns:` 的合法配置，接入前会被误判
  成"期望 object，实际 undefined"的阻断性错误。
- **`RuleFix.path` 显式给出相对路径时，`fromRuleIssue` 宁可不给 fix 也不猜错
  位置**：`RuleFix.path` 省略时默认等于 issue 自身已解析好的绝对路径（这条能
  正确处理）；但显式给出时，它是相对于规则自身作用域的（普通规则相对模块根，
  通配规则相对命中的元素），到了扁平的 `RuleIssue` 这一层已经无法反推是哪一种、
  元素落在文档的哪里。给出一个可能指错地方的"一键修复"比不给修复更糟——现在
  六个模块的 `validation.rules.json` 都没有用到 `fix`，这条分支目前没有真实内容
  触发，如实记录在 `issue.ts` 的注释里，不强行猜测。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-01 证据列补 schema 阶段用例；FR-VAL-05 证据列补
「不阻断」用例；`pipeline.ts` 的 v0.3.0 TODO 注释消除。

---

### #13 validator `security` 阶段（FR-VAL-04）

**目标**：对开放 LAN、空 Controller Secret、跳过证书校验等给出安全警告。

**覆盖**：FR-VAL-04（PRD §8.7 流水线阶段 6）。

**依赖**：#12。

**涉及文件**

- 新建：`packages/validator/src/security.ts`、`src/security.test.ts`
- 修改：`packages/validator/src/pipeline.ts`（注册 `securityStage`）、`src/index.ts`

**实现要点**

- **判定依据优先取 Schema 的 `safety` 元数据**（#8 已给 `allow-lan` 标 `caution`），
  只有元数据表达不了的组合条件才写进本阶段的代码。这样「哪些字段危险」仍然是 Bundle
  可更新的知识，而不是编译进应用的清单。
- 本版本覆盖三类（版本文档点名）：`allow-lan: true` 且 `bind-address` 为通配；
  `external-controller` 已设置但 `secret` 为空或缺失；`skip-cert-verify: true`。
- **严重级是 `warning` 不是 `error`**——安全警告不该阻断导出，否则用户会被迫用无效草稿
  通道绕过，反而更糟。这条要有用例。
- 消息只带路径与字段名，**绝不回显 secret 的值或 controller 地址**（NFR-SEC-03），
  用真实值驱动否定用例。
- 阶段顺序：`syntax` → `schema` → `security`。语法阻断时同样短路。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-04 由 Todo → **Done**（指向 `security.test.ts`）；
`pipeline.ts` 的 `security`（v0.3.0）注释消除。

**执行时修正**：

- **放弃"通用 `safety` 元数据扫描"，改为三项检查各自直写 `document.getIn()`**。
  计划原本设想"判定依据优先取 Schema 的 `safety` 元数据"，但 `allow-lan`/TUN
  端口等标了 `caution` 的字段都在 `general`/`inbound`——document-root 直属字段，
  `buildFormPlan` 能触达；而今天唯一存在的 `dangerous` 级字段
  `skip-cert-verify` 长在 `proxies[]`（数组）和 `proxy-providers.*.override`
  （映射）里，两者都是判别式联合形状，`buildFormPlan(module, 整份文档)`
  端到端还穿不过去（#9-#12 反复确认、明确留给 #14 的同一结构性缺口）。一个
  "扫描所有 `safety` 元数据字段"的通用机制在今天的代码状态下会对
  `skip-cert-verify` 静默失效——比"没有这个机制"更危险，因为它看起来在
  工作。改为三项检查各自直接 `document.getIn(path)` 读取已知路径，`proxies[]`
  按下标遍历、`proxy-providers` 按 key 遍历，牺牲了"新增危险字段只需改
  Schema 元数据"的可扩展性，换取三项检查在今天就真实生效。#14 解决数组
  /映射联合的表单渲染问题后，可以重新评估是否值得把这三项检查迁回元数据
  驱动。
- **`bench/corpus.test.ts` 的语料 sanity 测试断言收窄，不算放松测试**。
  `generate-large.ts`（未改动）的语料模板本就含 `allow-lan: true` + 通配
  `bind-address`、无 `secret` 的 `external-controller`，是刻意贴近真实
  Mihomo 默认配置的样本。新增的 `securityStage` 正确识别出这两处，使旧断言
  `expect(issues).toEqual([])` 不再成立。该测试的名字是「parses with no
  syntax issues and is not blocking」——它的职责从来不是"零 issue"，而是
  "语法干净、不阻断"，旧断言意外超出了自己的声明范围。改为
  `expect(issues.some(i => i.module === 'yaml')).toBe(false)` +
  `expect(hasBlockingIssues(issues)).toBe(false)`，与测试名字对齐；不是为了
  让测试通过而删断言或降阈值。

---

### #14 Web 表单页：模块渲染与基础 / 高级模式切换

**目标**：把 Registry 解析出的模块渲染成表单页，并支持基础 / 高级模式切换。

**覆盖**：FR-SCHEMA-01（真实模块）、PRD §7.4；NFR-PERF-03（字段编辑 < 100ms）；
退出条件 6。

**依赖**：#1、#12。

**涉及文件**

- 新建：`apps/web/src/form/ModuleFormPage.tsx`、`src/form/ModuleFormPage.test.tsx`
- 修改：`apps/web/src/project/ProjectPage.tsx`、`src/i18n/{zh-CN.json, en.json}`

**实现要点**

- **页面不认识任何模块 id**：模块列表来自 Registry（#5 的静态断言会拦住硬编码）。
  表单渲染直接复用 `@mcs/form-renderer` 的 `SchemaForm`，页面只负责布局与模式切换。
- **先决定 `general`/`inbound` 共享文档根（`root: []`）怎么渲染，再决定跨模块
  未知字段怎么去重**（#8 的执行时修正明确留给本片）：两个模块各自一个独立
  `SchemaForm` 会让对方已声明的字段互相误判成"未知"；合并成一个表单则从根上
  不存在这个问题，但需要确认这与"模块列表来自 Registry、页面不认识模块 id"
  是否冲突。选定方案后，如果仍是"各自独立表单"，再给 `buildFormPlan` 加一个
  忽略特定 key 的选项（由页面基于 Registry 里同根模块的 Schema 算出）。
- **`proxies`（及 #11 的 `proxy-providers`）需要"数组元素→多个表单"的渲染，
  今天的 `buildFormPlan` 没有**（#9 的执行时修正明确留给本片）：`config.schema.json`
  描述的是单个元素（`oneOf` 在 schema 根），`buildFormPlan(module, 整份文档)`
  端到端还跑不通（`proxies:` 是数组，不是 record）。本片要做的是遍历
  `documentValue.proxies`、对每个元素做"包装→`buildFormPlan`→拆包装、把返回的
  `PlannedField.path` 重新加上 `['proxies', i]` 前缀"，`packages/schema-builtin`
  的 `builtin.test.ts` 里已经有一份可直接抄的包装手法（`planOneProxy`）。
- 表单编辑经 Worker 的 `applyPatch` 消息落到文档上——主线程仍不持有
  `MihomoYamlDocument`，v0.2.0 的 Worker 边界断言必须继续绿。
- **退出条件 6 的核心断言**：基础模式下切到高级模式再切回来，导出文本**逐字节不变**；
  且高级模式独有字段在基础模式下不出现在 DOM 但仍存在于文档。这是 v0.2.0
  `form-plan.test.ts` 那条用例在真实模块 + 真实文档上的复验，必须用真实导出文本断言，
  不能只断言 plan 对象。
- 实验性 / 已废弃 / 平台专用字段的标识**不能只用颜色**（NFR-A11Y）：徽章要有文本或
  图形。`form-renderer` 已渲染 `data-badge`，本片补样式与 i18n 文案并断言非颜色可感知。
- NFR-PERF-03 的「常规字段编辑 < 100ms」：用注入时钟测「输入 → `onChange` → 补丁发出」
  这段主线程耗时，**不测 Worker 往返**（那是防抖窗口，已有断言）。数字记进
  `docs/releases/plans/v0.3.0-perf-baseline.md`。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-01 证据列补真实模块用例；NFR-PERF-03 → Done；
新建 `v0.3.0-perf-baseline.md`。

**执行时决策与修正**：

- **共享文档根决策（先问用户再动手）**：`general`/`inbound` 共享 `root: []` 时，
  `buildFormPlan` 会把对方已声明的字段真实标成 `unknown`（读 `form-plan.ts` 第
  191-211 行确认，不是猜测）。计划文本把方案选择明确留到本片决定，这是一个有
  持久 API 面影响的架构分叉（是否给 `buildFormPlan` 加新选项 vs. 运行时合并两份
  独立作者的 JSON Schema），征询用户后选定**独立表单 + `buildFormPlan` 加
  `additionalKnownPaths` 选项**：每个模块仍各自一个 `SchemaForm`，新增的
  `computeKnownPaths`（对每个模块跑一次 `buildFormPlan`，取并集）算出的已知路径
  集合，原样传回**每一个**模块自己的 `buildFormPlan` 调用——包括模块自己已声明
  的字段也传回去，因为一个真正被自己 schema 声明的属性永远不会走到"额外字段"
  分支，所以传全集永远安全，不需要先分组"哪些模块共享哪个根"。放弃"合并成一个
  表单"是因为需要在运行时合并两份独立作者的 JSON Schema（`properties`/
  `required`/`additionalProperties`/`$defs` 命名空间都有真实冲突风险），比新增
  一个小的、可选的、向后兼容的 `buildFormPlan` 选项风险大得多。
- **`proxies`/`proxy-providers` 数组渲染**：把 `builtin.test.ts` 的测试专用包装
  手法（`planOneUnionItem`）转正为生产代码——`schema-core` 新增
  `buildArrayFormPlan`（包装单个元素成 `{item: element}`、探测模块 `root: []`
  规划、再把结果每条路径的 `'item'` 前缀重写成 `[...root, index]`，含
  `variant.discriminatorPath` 与递归 `children`）与 `isArrayEntryModule`（纯结构
  判定：schema 根是 `oneOf`/`anyOf` 且无 `type`/`properties`，不认识任何具体
  模块 id）；`form-renderer` 新增 `SchemaArrayForm`，页面按
  `isArrayEntryModule(module)` 在两个组件间二选一分派，同样不认识任何模块 id。
- **`IssueFix` 扩展 `set` 变体（未在计划文件列表内，必要的范围外增补）**：
  `SchemaForm.onChange(path, value)` 对 `key-value`/`tags`/`list` 等非标量控件
  产生数组/对象值，而原有 `IssueFix` 只有 `set-scalar`（`value` 限定
  `JsonPrimitive`）。没有放宽整个接口的 `value` 类型（会连带削弱
  `set-scalar`/`remove`/`rename`/`append` 四种校验器建议 fix 原有的编译期收窄，
  `issue.test.ts` 已有一条"拒绝非标量 fix 值"的编译期断言证明这点），而是改成
  判别式联合，新增 `{kind:'set'; path; value?: unknown}` 一支，原四种 kind 的
  形状逐字节不变。`protocol.ts` 的 `applyIssueFix` 加 `case 'set'`，映射到
  `MihomoYamlDocument.setIn`（而非 `setScalarIn`）。
- **Worker 新增 `value` 消息 + `ParseResponse` 补 `value` 字段（未在计划文件
  列表内，必要的范围外增补）**：渲染表单需要文档当前值，但主线程不能持有
  `MihomoYamlDocument`（v0.2.0 边界）。`parse` 响应顺带补 `value:
parseResult.document?.toJS() ?? null`（`YamlEditor` 已经在跑这次 parse，零
  多余往返，新增 `onValueChange` 回调把值上抛）；`applyPatch` 之后没有免费的
  parse 可以顺路补值，所以另加一个独立的 `value` 请求，`ProjectPage` 的
  `handleDocumentFieldChange` 在 `applyPatch` resolve 后并发拉 `value()` 和
  `serialize()`。
- **Worker 的 `runPipeline` 调用真正接入 `RESOLVED_MODULES`（未在计划文件列表
  内，必要的范围外增补，但直接决定 #12/#13 的成果是否在真实 app 里生效）**：
  `protocol.ts` 此前调用 `runPipeline({parse})` 从不传 `modules`——`schemaStage`/
  `securityStage`（#12/#13 已交付、已测试）在真实运行的 app 里从未真正跑起来
  过，只在各自包自己的测试里跑过。新增
  `const RESOLVED_MODULES = createRegistry(builtinAsStoredBundle()).modules()`
  （Worker 加载时算一次）传给每次 `runPipeline` 调用，`protocol.test.ts` 新增
  两条真实断言证明 `rules`（六个 P0 模块都不认领的真实字段）被正确标成
  `unknown-field`。副作用：`apps/web` 其余处处理"issues 应为空数组"的既有测试
  中，凡 fixture 含 P0 模块声明范围之外字段的，断言从"零 issue"改成"无阻断/无
  语法 issue"，与 v0.3.0 #13 处理 `bench/corpus.test.ts` 的方式一致——修正的是
  意外超出自身声明范围的断言，不是放松测试。
- **`builtinAsStoredBundle` 从 `schema-registry/src/store.ts` 私有函数转正为
  导出函数**：Worker（校验）与主线程（渲染）都需要"解析出内置 Bundle 的模块
  列表"，且都不该走 `resolveActiveBundle` 的完整异步 store-and-verify 流程——
  v0.3.0 没有 Bundle 安装 UI，内置 Bundle 是唯一可能激活的 Bundle，为一个还不
  存在的能力（安装其他 Bundle）先做异步等待/加载态是不必要的复杂度。两处各自
  独立调用（Worker 与主线程是两个不同 JS 执行上下文，运行时值不能共享），复用
  的是同一个纯函数，不是同一个实例。
- **`ModuleFormPage` 独立成 `ProjectPage` 的新章节，不嵌进 `YamlEditor` 的
  占位 `<fieldset>`**：`YamlEditor.tsx` 自己的"结构化视图"占位 `<fieldset
disabled={frozen}>` 看起来像是特意留的挂载点（旧注释写"v0.3.0 加的任何真实
  表单字段都继承这里的 disabled 行为"），但把表单渲染塞进去会显著扩大
  `YamlEditor` 自身职责（还需要 `modules`/`value`/`onFieldChange`/新 Worker
  方法），而计划文件列表明确只列了 `ProjectPage.tsx`（改）而非 `YamlEditor.tsx`
  （改）。选择保守方案：`ModuleFormPage` 作为 `ProjectPage` 里紧邻
  `YamlEditor` 的独立 `<section>`，`YamlEditor` 自己的占位文字原样保留——纯
  JSX 位置选择，不涉及数据模型，以后要挪很容易。
- **`ModuleFormPage` 对 `value === null` 单独判空，不复用"零模块"分支**：
  `modules` 由 `useState` 懒初始化一次算出，从不为空数组（生产环境恒有六个真实
  模块），但 `documentValue` 在项目刚选中、`YamlEditor` 首次 parse 落地前是
  `null`——若不做这个判断，表单会先闪一遍 schema 默认值（如 `mode: rule`），
  跟真实文件内容不一定一致，等真实 parse 落地后再"纠正"，观感上是错的、写测试
  时也是假信号（本片 `ProjectPage.test.tsx` 的集成测试最初就是被这个假信号
  坑了一次——`waitFor` 等的是模块 section 出现，但没值也照样出现，`applyPatch`
  在文档还不存在时就打过去，得到 `NO_DOCUMENT`，靠一个临时的
  `_scratch-debug.test.tsx` 打印真实消息序列才定位到）。`value: null` 与
  `protocol.ts` 里"文档语法阻断"的 `value` 也复用同一哨兵值（`handleParse` 对
  两种情况都发 `null`），两种情况都没有文档可规划字段，合并处理是对的，不是
  巧合的偷懒。
- **顺带修好三处发现的、非本片引入的 i18n 未接线 bug**：`form-renderer/src/
index.tsx` 的 `FieldRow` 里 `badge.deprecated`/`badge.experimental`/
  `badge.danger`/`link.officialDocs` 四处渲染的是字面量 i18n key 文本，不是
  `translate(...)` 调用结果——计划文本明确点名"本片补样式与 i18n 文案"，前三个
  正是计划要求的范围；第四个（`link.officialDocs`）是真实浏览器人工验证时
  肉眼发现的同类 bug，同一个函数、同一个修法，顺手一起修。`controls.tsx`
  里同类的第四、五、六处（`field.variant.unmatched`、`SecretControl` 的
  reveal 按钮与敏感提示）**没有**在本片顺手修——`field.variant.unmatched`
  被一条早期切片写的、断言这段字面量文本的测试锁定，另外两处需要先确认
  `ControlProps` 能不能拿到 `translate`，改动面已经超出"顺手"范围，留给独立的
  后续任务。
- **真实浏览器人工验证（.claude/launch.json 本地端口从 5173 改成 5400，
  本机 Windows 保留端口范围排除了 5173/5180，此改动不随本片提交）**：真实
  Vite dev server + 真实 Worker + 真实 IndexedDB，验证了字段编辑往返、模式
  切换逐字节不变（42→17 字段数真实变化，不是空操作）、危险徽章真实非纯色样式
  （边框/背景色计算值非 `undefined`）、`skip-cert-verify` 徽章真实中文文案。

**已知缺口，已各自拆分成独立后续任务**：`hosts` 一类自由映射（`key-value`
控件）字段的具体条目会被 `schemaStage` 误判成 `unknown-field`（#12 遗留，本片
才因为 Worker 真正接入 `RESOLVED_MODULES` 而在真实场景暴露，`severity:'info'`
非阻断）；`controls.tsx` 剩余的三处 i18n 未接线；v0.2.0 性能基线文档自己早就
预告的"首轮校验耗时接近零"现在不成立了（本片真正把模块接进了 `runPipeline`），
需要重新采样。

---

### #15 Worker `undo` / `redo` 协议与表单编辑历史栈（FR-PROJ-04）

**目标**：补上 v0.2.0 明确挂账的缺口——表单编辑与引用改名走同一条历史栈，且真的能撤销。

**覆盖**：FR-PROJ-04（转 Done）。

**依赖**：#14。**本片是 E3 的落点**，v0.2.0 追踪表第 63 行的缺口指向它。

**涉及文件**

- 修改：`apps/web/src/worker/protocol.ts`、`src/worker/protocol.test.ts`（新增 `undo` / `redo`
  一对请求响应，Worker 内持有 `HistoryStack`）
- 修改：`apps/web/src/worker/client.ts`、`src/project/ProjectPage.tsx`（壳层按钮接真实消息）

**实现要点**

- **`HistoryStack` 放在 Worker 内**，与它已持有的 `MihomoYamlDocument` 放在一起——
  主线程持有文档就打破了 v0.2.0 的 Worker 归属断言。v0.2.0 计划 #11 的「执行时修正」
  已经把这个设计写清楚了，本片照做。
- `applyPatch` 处理器用 `HistoryStack.record()` 包裹，**一次表单编辑 = 一个历史项**；
  连续同路径标量编辑按既有合并规则合并。
- 响应里回传 `canUndo` / `canRedo`，主线程按钮禁用态由此驱动（壳层已在 v0.2.0 #11 就位）。
- **核心断言**：真实表单编辑 → `undo` → 导出文本与编辑前**逐字节相等**。v0.2.0 只能在
  引擎层证明（`history-integration.test.ts`），本片第一次能在应用层端到端证明。
- 撤销边界：空栈撤销、重做后新编辑截断重做分支——引擎层已有用例，本片只需断言协议层
  正确转发失败而不是抛异常。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-PROJ-04 由 Partial → **Done**（指向 `protocol.test.ts`
的 undo/redo 用例与应用层字节相等断言），并删除指向 v0.2.0 #13 的旧缺口说明。

**执行时决策与修正**：

- **`ProjectPageProps.historyStack` 注入属性整体删除**：`HistoryStack` 移入 Worker
  后，主线程不再需要（也不能再）持有一份自己的 `HistoryStack` 实例；`ProjectPage`
  自己的 `canUndo`/`canRedo` 改为普通 `useState`，完全由 Worker 每次响应回传的
  `canUndo`/`canRedo` 驱动。`ProjectPage.test.tsx` 里旧的"注入一个预先 `record()`
  过的 `HistoryStack`"测试手法整体废弃，改用真实 `WorkerClient` + 真实
  `handleWorkerRequest`（与 #14 的"module form wiring"测试同一手法）——理由很
  直接：撤销/重做现在是 Worker 内部真实状态机的行为，一个手工构造的假
  `HistoryStack` 已经验证不了任何有意义的东西。
- **`applyPatch` 的 `mergeKey` 取 `JSON.stringify(patch.path)`，对全部四种
  `IssueFix.kind` 一视同仁**：计划文本只点名"连续同路径**标量**编辑"，但
  `HistoryStack.record()` 的合并逻辑本身与操作类型无关，只看 `mergeKey` 是否
  相同——没有理由只给 `set-scalar`/`set` 两种 kind 开合并，`remove`/`rename`/
  `append` 用同样的路径粒度合并同样合理（用户对同一个字段做的连续操作，无论
  操作类型，通常都想当一步撤销）。
- **`handleParse` 最初设计为"每次 parse 无条件重置历史栈"，人工在真实浏览器里
  验证时发现这是错的，jsdom 测试全绿掩盖了这个问题**——这是本片最重要的一次
  发现：
  - **设计意图**：`parse`（v0.3.0 有一个 Worker 服务全部项目，不是一个项目一个
    Worker）应该在"这是一份新文档，不是旧文档的延续"时重置历史，最明显的例子
    是切换项目——不重置的话，切到项目 B 后点撤销可能改到项目 A 最后编辑的内容。
  - **实际的 bug**：`ProjectPage` 的 `handleDocumentFieldChange`/`applyHistoryResponse`
    每次都会把 Worker 刚返回的 `text` 原样写回 `configText`，而这正是
    `YamlEditor` 自己的 `text` prop——`YamlEditor` 内部的防抖 `useEffect` 见到
    `text` 变化，300ms 后无条件重新 `parse()` 一次，不管这次变化是不是用户自己
    敲的。于是**每一次表单编辑之后大约 300ms，历史栈都会被这次"回声重解析"
    静默清空**——用户编辑后如果等超过 300ms 才点撤销（现实中总是如此），
    撤销按钮虽然还显示可用（`ProjectPage` 的 `canUndo` state 是从 `applyPatch`
    的响应设的，从未被这次多余的 `parse` 响应更新过），点了却毫无效果。
  - **jsdom 测试为什么全绿**：本片最初写的所有 undo/redo 测试都是"编辑 →
    `waitFor` 按钮可用 → 立刻点撤销"，这个时间差通常在几十毫秒内，远小于
    300ms 的防抖窗口——快速的同步测试执行时机，恰好从未落进这个窗口。这正是
    "For UI or frontend changes, start the dev server and use the feature in a
    browser" 这条工作准则要拦的那类问题：类型检查和测试套件证明的是代码正确性，
    不是"一个真实用户会怎么用"这件事本身。
  - **修正**：`handleParse` 改为比较 `request.text` 与
    `state.parseResult?.document?.toText()`（当前文档已经产出的文本）——相等
    就是"Worker 自己编辑结果的回声"，不重置；不相等（项目切换、导入、真实的
    原始文本手打编辑）才是真正的新文档，重置。三种情况都验证过：
    `protocol.test.ts`「re-parsing the exact text the Worker already holds does
    NOT reset the undo stack」（回声不重置）、「a fresh parse resets the
    undo/redo stack...」（项目切换重置）；`ProjectPage.test.tsx` 新增一条专门
    等过 300ms 防抖窗口再点撤销的集成测试，先临时改回无条件重置版本确认这条
    测试真的会失败，再改回修正版确认通过——不是巧合通过。

---

### #16 未知字段树与问题中心跳转到表单字段

**目标**：未知字段可见、可编辑、有官方文档搜索入口；问题可跳到表单字段而不只是 YAML 行。

**覆盖**：FR-VAL-02（跳转到表单字段）、FR-VAL-05（UI 侧）；PRD §8.3 高级配置、§12。

**依赖**：#14。

**涉及文件**

- 新建：`apps/web/src/form/UnknownFieldTree.tsx`、`src/form/UnknownFieldTree.test.tsx`
- 修改：`apps/web/src/issues/IssuePanel.tsx`、`src/issues/IssuePanel.test.tsx`

**实现要点**

- 未知字段树的数据源是 `FormPlan.unknownFields`（已存在），**不重新遍历文档**。
  展示为可折叠树，值只读呈现 + 可跳到 YAML 行编辑。
- **官方文档搜索入口是一个链接，不是一次抓取**：`apps/web` 不得发起任何网络请求
  （NFR-SEC-01），入口只构造 URL 并交给浏览器打开，且**URL 里不带任何配置值**
  （只带字段名）。这条要有用例——把配置值塞进查询串是隐私泄漏，v0.2.0 的
  `no-network-egress` job 拦不住它。
- FR-VAL-02 的表单跳转：`ValidationIssue.path` → `toPointer(path)` → 表单里 `data-field`
  属性精确匹配 → 滚动并聚焦。`form-renderer` 已经用 JSON Pointer 作为
  `data-field` / `id`，本片直接复用，不新建映射表。
- 问题既有 `range` 又有 `path` 时**同时提供两个入口**（跳 YAML 行 / 跳表单字段），
  由用户选，不替用户猜；只有其一时只显示可用的那个，两者都无时保持非交互
  （v0.2.0 已有这条口径的用例，沿用）。
- 未知字段标 Unknown 不标 Error，UI 侧同样不得渲染成错误样式。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-VAL-02 证据列补表单跳转用例；FR-VAL-05 证据列补
未知字段树与「文档链接不含配置值」用例。

**执行时决策与修正**：

- **`UnknownFieldTree` 的数据聚合放进 `schema-core`，不放进 `apps/web`**：新增
  `collectUnknownFields(modules, documentValue, options)`，对象模块复用
  `buildFormPlan().unknownFields`，数组模块（`proxies`/`proxy-providers`）用
  `buildArrayFormPlan` + 内部的 `flatten` 助手手动收集——与 `buildFormPlan`
  同一个包，同一套判别式联合处理逻辑，不在 `apps/web` 里重新实现一遍"数组模块
  要特殊处理"的知识。
- **`computeKnownPaths` 发现并修复一个 v0.3.0 #14 就已上线、影响真实渲染结果
  的 bug（非本片引入）**：写 `collectUnknownFields` 的测试用例时（两个模块，
  一个 `root: []`，一个 `root: ['items']`）意外发现——`general`（`root: []`）
  的表单此前会把 `dns`、`sniffer`、`proxies` 这些其他模块自己的文档根 key
  全部错误标成"未知字段"。根因：`computeKnownPaths` 只收集了每个模块*已声明
  子字段*的路径（如 `['dns', 'enable']`），从未把模块自己的根路径本身
  （`['dns']`）计入已知集合——嵌套对象模块的根路径从来不是它自己 `plan.fields`
  里的一条路径，数组模块的整个根更是连 `buildFormPlan` 都穿不进去
  （`planObject` 对着一个数组、`isRecord` 判否，直接跳过）。修复：
  `computeKnownPaths` 额外把每个模块的 `manifest.root`（非空时）本身也计入
  已知集合——没有任何字段应该合法地把别的模块的根路径当成自己的属性，这条
  规则不需要理解数组/不透明作用域的内部结构就能成立。真实浏览器用真实六个
  P0 模块组合复验：`general` 的表单不再显示 `dns`/`sniffer`/`proxies` 的
  伪未知字段。
- **`IssuePanel` 的跳转重新设计成两个独立入口，不是"range 优先、path 兜底"**：
  计划文本明确要求 `range`/`path` 各自独立决定各自的入口是否出现，不是二选一
  兜底关系——`handleJump` 改名 `handleJumpToLine`（含义更准确），新增独立的
  `onJumpToField` 走 `path` 直接跳，不经过 `client.locate`（跳表单字段不需要
  文本 range）。DOM 结构相应从"整行是一个按钮"改成"非交互的消息 `<span>` +
  0～2 个动作按钮"，`aria-label` 保留"严重级别: 消息"前缀、按钮再各自加
  "— 跳到 YAML 行"/"— 跳到表单字段"后缀，让屏幕阅读器用户在多条问题的列表里
  分清楚在哪一条上、按的是哪个动作。
- **`ModuleFormPage` 转成 `forwardRef`，暴露 `jumpToField(path)`**：`YamlEditor`
  已有的 `jumpToRange` 是现成的接线范式，直接照搬——按 `data-field` 属性
  （`form-renderer` 早就用 JSON Pointer 当这个属性的值，本片直接复用）找到
  目标行，`scrollIntoView` 后聚焦。**执行时人工在真实浏览器验证才发现的真实
  bug**：对一个未知字段（渲染成只读 `<output>`，容器 `<div>` 里没有任何
  input/select/button 等可聚焦元素）调用容器的 `.focus()` 是静默无效的——
  `document.activeElement` 根本不动，浏览器不报错，jsdom 测试当时也没测这条
  路径所以全绿。修复：找不到可聚焦控件时，给容器临时打上 `tabindex="-1"`
  （只让它能被程序聚焦，不进入 Tab 键序）再聚焦，之后补了三条 jsdom 回归
  用例（真实可聚焦控件、无控件的未知字段、路径不存在的空操作）复验，同时
  在 `ModuleFormPage.test.tsx` 顶部给 `Element.prototype.scrollIntoView`
  打了个 `vi.fn()` 桩——jsdom 压根没实现这个方法，这纯粹是测试环境的缺口，
  不是生产代码需要防御的场景。
- **测试环境笔记：`getByRole('button', { name: string, exact: false })` 在本
  环境下对含中英混排（如"跳到 YAML 行"）的 `aria-label` 不可靠**：手工验证过
  同一段文本、同一次渲染，直接用字符串 `.includes()` 检查能命中，但
  `getByRole` 的 `exact: false` 字符串匹配却报"找不到"；换成等价的 `RegExp`
  立刻能命中。没有深挖 testing-library 内部实现，直接把本片新增/改动的所有
  `getByRole(..., { name, exact: false })` 用例统一换成 `RegExp` 匹配（新增
  `byButtonName()` 小助手），后续如果还在别处遇到同类"找得到文本、找不到
  role"的诡异失败，值得先怀疑这一条。

---

### #17 订阅 URL 管理与本地 Provider 文件预览

**目标**：订阅 URL 可填、可改、默认隐藏、可显式复制；本地 Provider 文件可导入预览。

**覆盖**：PRD §8.11 P0；NFR-SEC-02 复验；ADR-005。

**依赖**：#11、#14。

**涉及文件**

- 新建：`apps/web/src/form/SubscriptionField.tsx`、`src/form/SubscriptionField.test.tsx`
- 修改：`apps/web/src/import/ImportPanel.tsx`（本地 Provider 文件预览入口）、
  `src/i18n/{zh-CN.json, en.json}`

**实现要点**

- 订阅 URL 走既有 `secret` 控件的遮罩语义（#11 已标 `sensitive`），本片补的是
  **显示与复制两个明确操作**：默认遮罩，「显示」需一次点击，「复制」是独立按钮且
  不先显示明文。用例断言遮罩状态下 DOM 里**不存在**明文 URL（不是靠 CSS 隐藏）。
- **Web 端不承诺抓取订阅（ADR-005）**：本片不加任何 `fetch`。跨域失败不绕过——
  因为压根不发请求。UI 上要有一句明确说明（i18n key），避免用户以为是 bug。
  用例用源码扫描断言本片新增文件不引用 `fetch` / `XMLHttpRequest`
  （与 v0.2.0 `ImportPanel.test.tsx` 的「无可写文件句柄」断言同一手法）。
- 本地 Provider 文件预览：复用 `ImportPanel` 的 `File.text()` 路径，解析后只做
  **结构预览**（节点数量、字段形状），**不写入项目文档**——PRD §8.11 说的是「导入本地
  Provider 文件进行结构预览」，不是合并进配置。
- 预览内容不得包含节点密码 / UUID 明文（NFR-SEC-02 / SEC-03），要有否定用例。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表新增 §8.11 小节（订阅 URL 管理 → Done、本地 Provider
预览 → Done、P1 Android 原生请求 → Todo）；NFR-SEC-02 证据列补真实订阅字段用例。

**执行时决策与修正**：

- **前置阻塞缺陷：`buildArrayFormPlan` 只认列表，`proxy-providers:` 上游真实形态是
  映射**。对照 vendor 的 `comprehensive.yaml`（`proxy-providers:` 下每个订阅以
  `provider-a`/`provider-b` 这样的名字为 key，不是数组）确认后发现：#14 写
  `buildArrayFormPlan` 时只在 `Array.isArray(entries)` 为真时规划，`proxy-providers`
  一直落进 `if (!Array.isArray(entries)) return [];` 分支，静默规划成空表单——
  `isArrayEntryModule` 只能从 schema 形状（裸 `oneOf`，无 `type`/`properties`）判断
  "这是集合的单条目模式"，判断不出集合本身是列表还是映射，而 `proxies` 恰好是列表、
  `proxy-providers` 恰好是映射，两者共享同一段判别逻辑。修复：`buildArrayFormPlan`
  内部新增 `collectionEntries()`，同时接受 `Array`（下标做 key）与普通 object
  （`Object.entries` 做 key），返回 `[PathSegment, unknown][]`——`ConfigPath` 本来就
  混用字符串/数字下标，无需新类型。这是本片能继续往下走的前提：不先修这个，
  `SubscriptionField` 根本没有机会被渲染出来。
- **`SubscriptionField` 为什么是独立控件而不是给通用 `secret` 控件加复制按钮**：
  计划原文「订阅 URL 走既有 secret 控件的遮罩语义……本片补的是显示与复制两个明确
  操作」容易读成"给 SecretControl 加个复制按钮"，但 ADR-005 的免责声明（本工具不
  自动抓取订阅）是订阅 URL 专属的产品文案，塞进通用 `SecretControl`（`password`/
  `uuid`/`private-key` 等字段共用）没有意义。改为：`ui.schema.json` 给
  `proxy-providers.http.url` 一个新的显式 `control: 'subscription-url'`（`ControlType`
  新增枚举值，`inferControl` 从不会自动推断出它，只能显式声明），`apps/web/src/form/
SubscriptionField.tsx` 实现为独立组件，经 `ModuleFormPage.tsx` 新增的
  `APP_CONTROLS` 映射（走 `SchemaForm`/`SchemaArrayForm` 既有的 `controls` 覆盖入参）
  注入——不改 `packages/form-renderer` 一行，`FR-SCHEMA-05` 的"模块可加字段无需改
  应用代码"对这条同样成立（新增的是 `ui.schema.json` 数据，不是新的应用分支）。
  副作用：`packages/schema-builtin` 的 `builtin.test.ts` 那条断言
  `subscriptionUrl?.control` 的用例，期望值从 `'secret'` 改成 `'subscription-url'`。
- **复制按钮：Clipboard API 存在不等于能写入**。真实浏览器手测「点击复制」后按钮
  文案没有变成「已复制」，`navigator.clipboard.writeText` 抛出
  `NotAllowedError: Write permission denied`——自动化浏览器环境的权限策略拒绝了
  写入，即使 `navigator.clipboard` 对象本身存在（`typeof navigator.clipboard ===
'object'`）。这不是自动化环境独有的边界情况：真实用户在权限被拒、文档失焦等场景
  下同样可能撞到。`handleCopy` 原先没有 `try/catch`，`await` 失败会让
  `handleCopy()` 返回的 Promise 变成 rejected，`onClick={() => void handleCopy()}`
  的 `void` 只压制了 TS 的"未处理 Promise"检查，压不住浏览器真正的
  unhandled-rejection 事件——控制台确认出现过 `Uncaught (in promise)`。修复：包一层
  `try/catch`，失败时静默不显示"已复制"确认，不抛出、不弹错误 UI（复制是锦上添花
  的便利操作，输入框本身随时可以手动选中复制）。`SubscriptionField.test.tsx` 新增
  "拒绝时安静失败"用例（`mockRejectingClipboard`）复验。
- **本地 Provider 文件预览的内容设计：白名单，不是黑名单**。计划原文"预览内容不得
  包含节点密码/UUID 明文，要有否定用例"最直接的实现是"遍历字段名，命中
  `SENSITIVE_KEY` 正则的不显示值"——但正则黑名单永远可能有遗漏。改为反过来设计：
  `ProviderPreviewNode` 类型只有三个字段——`name`/`proxyType`（显式读取这两个、且
  只有这两个字段的*值*）与 `fieldKeys`（`Object.keys(entry)`，只有 key 没有
  value）。协议里任何其他字段（`password`/`uuid`/`obfs-password`……）的值永远不会被
  这段代码读出来，不依赖"正则是否覆盖全了"——类型形状本身就是安全边界。落地在
  `apps/web/src/worker/protocol.ts` 新增的 `previewProvider` 消息
  （`handlePreviewProvider`/`summarizeProviderFile`），沿用 `handleParse`
  同一个 `MihomoYamlDocument.parse`（同样的大小/深度限制与安全姿态），但完全不碰
  `state.parseResult`/`state.historyStack`——预览的 Provider 文件与当前打开的项目
  无关，这是 PRD §8.11"结构预览、不写入项目文档"的字面要求。`protocol.test.ts`
  「never includes a sensitive value anywhere in the response...」用真实 `uuid`/
  `password` 值对整个序列化响应做否定断言复验。
- **`ImportPanel.tsx` 新增的 NFR-REL-04/ADR-005 源码扫描踩了一个自证陷阱**：仿照
  既有"扫描源码确认不引用可写文件 API"的手法，新写了一条扫描"不引用 fetch/
  XMLHttpRequest"的用例，第一次跑就红——不是代码真的引用了这两个 API，是解释这条
  规则的文档注释里*写了* "fetch"/"XMLHttpRequest" 这两个词，扫描把注释也算进去了。
  改法：注释里不再逐字出现这两个标识符，用"network request of its own"这样的表述
  代替，测试与注释互不干扰。
- **执行时发现并修复两个与本片改动直接相关、真实浏览器手测才暴露的严重既有缺陷**
  （均非本片引入，是本片第一次让 `proxy-providers`/`proxies` 真正端到端跑通完整
  Worker 校验流水线时才暴露）：
  1. `schemaStage`（`packages/validator`，#12 起接入真实模块）对 `isArrayEntryModule`
     模块从未正确处理过——直接把整个集合（列表或映射）传给
     `validateValue(scope, module.schema, ...)`，而 `module.schema` 描述的是集合的
     单个条目、不是集合本身，`oneOf` 永远匹配不上，**任何包含至少一个真实代理节点
     或订阅的文档，导入时都会被判定为阻断性错误**——这实际上意味着本工具的核心用途
     （导入一份带节点的真实 Mihomo 配置）从 #12 起就是坏的，只是从未被任何自动化
     测试或人工验证撞见（v0.2.0 的 `DEFAULT_PROJECT_CONFIG_TEXT` 只有 `mode: rule`
     一行；既有测试要么不装 `PROXIES_MODULE`，要么绕开 `schemaStage` 直接调用
     `validateValue`）。`pipeline.test.ts` 里那条"never blocks on unknown fields no
     matter how many"用例的注释其实早就写着"array-of-entries planning is #14's
     job"——#14 只交付了渲染层（`buildArrayFormPlan`/`ModuleFormPage`），校验层这半
     从未有人接手，本片补上。修复思路与 `buildArrayFormPlan` 同源：复用它逐条目做
     判别式匹配，只对匹配到某个 P0 协议分支的条目跑 `validateValue`/
     `evaluateRules`（`basePath` 指向该条目自身的绝对路径，不是整个集合的根）；
     未匹配任何分支的条目（P1/P2 协议，设计上就不在 P0 建模范围）不当作违规，直接
     走未知字段通路——这一步刻意不用"每个条目都必须匹配某个分支"，否则会把 P1/P2
     协议节点从"降级为未知字段"变成"直接拒绝导入"，反而更差。判别式的 discriminator
     字段（如 `type`）被 `planVariantChildren` 有意排除在 `.children` 之外（用
     `field.variant` 表达，不重复列成子字段），`knownPaths` 汇总时如果不特殊处理会
     把 `type` 也误判成未知字段——已用 `field.variant.discriminatorPath` 显式补上。
     `flattenFields`（原 `form-plan.ts` 内部私有的 `flatten`，本片改名导出）复用
     给 `schemaStage` 把 `buildArrayFormPlan` 返回的每条目字段树摊平成已知路径集合。
  2. `collectUnknownFields`（v0.3.0 #16）逐模块把各自未知字段直接拼进同一个数组，
     从不去重——`additionalKnownPaths` 只抑制"*某个*模块自己声明的字段"，一个
     *所有*模块都不认领的叶子（这个函数存在的本来目的）在 general/inbound 这类
     共享文档根的模块组合下会被每个模块各自独立报告一次，导致同一路径在结果里出现
     两次；`UnknownFieldTree` 用路径当 React `key`，因而触发"两个子节点用了同一个
     key"的渲染警告（真实浏览器手测导入含未识别字段的文档时才看见）。修复：
     `collectUnknownFields` 内部按序列化路径去重，同一叶子只保留第一次出现。
     两处都在 `docs/requirements-traceability.md` 的 FR-VAL-01/FR-VAL-05 行详细记录。
- **一个 testing-library 踩坑记录，供以后类似情况参考**：`ImportPanel.tsx` 里
  `{node.proxyType ?? t('providerPreview.unknownType')}` 原先是 `<strong>` 与
  `<div>` 之间的一段裸文本（前面还有 `{' — '}`），`getByText(t('providerPreview
.unknownType'))` 找不到——不是因为文本没渲染出来，是这段裸文本与相邻的 `{' — '}`
  同属 `<li>` 的直接子文本节点，testing-library 默认按"最近包裹元素的完整文本"匹配，
  混进了兄弟文本节点后整体对不上查询字符串。改法：给这段文本单独包一层 `<span>`，
  查询就能精确匹配了；顺带也让"名称—类型"两段视觉上更好区分。

### #18 FR-SCHEMA-04 字段元数据全量断言

**目标**：把「每个字段可关联官方文档、示例、平台、引入版本、废弃版本和安全等级」
从「类型里有」变成「六个模块真的填了且被断言」。

**覆盖**：FR-SCHEMA-04。

**依赖**：#10、#11（六模块齐备才能全量）。

**涉及文件**

- 新建：`packages/schema-builtin/src/metadata.test.ts`
- 修改：六个模块的 `ui.schema.json`（补齐缺失的元数据）

**实现要点**

- 断言是**表驱动 + 覆盖性**的，与 `packages/ui/src/contrast.test.ts` 同一手法：
  遍历六个模块的每个字段，逐项检查元数据完备性。新增字段不填元数据即红——
  这条是防止后续模块悄悄漏填的关键。
- 完备性口径要分级，不能一刀切要求全填：`docs` 与 `safety` **必填**；
  `since` / `deprecatedSince` **仅当上游确有版本信息时必填**（依据来自 #3 的字段清单，
  不允许凭空编造版本号）；`platforms` 仅对平台专用字段必填。口径写进测试文件注释。
- `docs` 链接必须指向官方文档域名且**不含任何查询参数携带的配置值**（与 #16 同一条底线）。
- 废弃字段的 `replacedBy` 若填了，必须指向本模块内真实存在的字段——要有断言，
  否则废弃提示会指向不存在的东西。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-SCHEMA-04 → **Done**（指向 `metadata.test.ts`）。
至此 FR-SCHEMA-01…06 全部 Done，退出条件 1 满足。

**执行时决策与修正**：

- **真实审计先行，再决定要不要改 `ui.schema.json`**：写断言之前先跑一次性脚本递归
  遍历六个模块共 109 个字段，结果是 `docs`/`safety` 已经 100% 齐备——`#6`–`#11`
  逐模块开工时显然已经把这两项当成隐性纪律在遵守，只是从未被正式断言过。计划原文
  「涉及文件」列的「修改：六个模块的 `ui.schema.json`（补齐缺失的元数据）」因此
  **实际未发生**——没有缺口可补。这与本片其余"审计后发现确有缺口"的模式相反，
  值得如实记录：不是每次审计都会找到问题。
- **`metadata.test.ts` 的测试用例直接从真实模块数据动态生成，不手写清单**：与
  `packages/ui/src/contrast.test.ts` 的手法有一处刻意的不同——`contrast.test.ts`
  的表是手写的 `CASES` 常量，需要额外一组「coverage guard」用例反向核对表没有漏掉
  `COLORS` 里的新增色阶；本片改为直接对每个模块 `collectFields(module.ui.fields)`
  的结果跑 `it.each`，覆盈性由构造方式保证，不需要单独的反向核对——新字段自动获得
  一条断言，没有"忘记把新字段加进清单"这一类缺口可能。
- **`docs`/`safety` 用 `it.each`（每字段一条独立用例，109+ 条断言分布在真实测试名里）
  而不是一个大 `for` 循环 + 一条 `expect(offenders).toEqual([])`**：两种写法本片都用
  到了（`replacedBy`/`deprecatedSince` 配对用的是后者）。`docs`/`safety`
  特意选 `it.each`：这两项是"每次新增字段都必须顺手做的动作"，失败时希望**在测试
  报告里直接看到是哪个字段**（`general.mode has both docs and a safety level`），
  不是先看到一个笼统的失败再去读 offenders 数组；`replacedBy`/配对检查是当前"零发生"
  的不变式，选哪种写法都无所谓可读性，用更简洁的 `for`+`offenders` 更合适。
- **`since`/`deprecatedSince` 口径：现在不为任何字段强制要求，理由写进了文件自身的
  注释而不是本文档**——`upstream.ts` 与六份 `config.schema.json` 均未记录任何字段的
  引入/废弃版本，本版本兼容档案（ADR-012）钉死单一 Mihomo 版本而非区间，没有真实
  版本号可写。唯一新增的强约束是"二者只在同时出现时才有意义"（一个只给了废弃时间
  没给替代字段的提示，或反过来，都不可行动）——这条不依赖任何上游研究，纯粹是内部
  一致性，可以现在就断言。
- **`replacedBy` 校验用真实故障注入复验，不是只看代码逻辑"应该"对**：临时把
  `general.mode` 的 `docs` 删掉、加一个指向不存在字段名的 `replacedBy`，确认两条
  断言（"docs 缺失"与"replacedBy 无效"）都正确变红且报错信息能定位到具体字段，
  随后原样恢复（`git diff` 确认改动后与改动前逐字节一致）——这是本片写的两条此刻
  仍是"零真实发生"的不变式（`replacedBy` 有效性、`deprecatedSince`/`replacedBy`
  配对）里，唯一能验证的方式：没有真实违规样例，只能自己临时制造一个再撤销。
- **`examples`（四类样例）核实后确认已在 `builtin.test.ts` 落地，未重复实现**：
  `docs/requirements-traceability.md` 的 FR-SCHEMA-04 行此前写着"全量断言见 #18"，
  容易读成"examples 也要等 #18"——核实后发现 `describe.each(MODULES)`（general/dns/
  sniffer/inbound）与 `proxies`/`proxy-providers` 各自的 describe 块里都已经有
  「lists all four example kinds with a path that exists on disk」，六个模块全覆盖，
  只是分散在早期几片（#6/#9/#11）里各自交付、从未被追踪表准确反映过。本片只更新
  了追踪表措辞，不新增 examples 相关测试。

---

### #19 Golden 五步测试与专项样例

**目标**：PRD §13.2 的五步 golden/round-trip 测试，未知字段语义保留率 100%。

**覆盖**：PRD §13.2、§14.1；退出条件 3。

**依赖**：#3（官方完整样例）、#10、#11（六模块齐备才谈得上「语义树一致」）。

**涉及文件**

- 新建：`packages/test-fixtures/fixtures/yaml/golden/*.yaml`（专项样例：注释、锚点、
  特殊字符串、IPv6、正则、多行密钥各一份）
- 新建：`packages/validator/src/golden.test.ts`（五步测试）

**实现要点**

- 五步严格按 PRD §13.2 逐条对应用例，**一步一条断言**，不合并：
  1. 导入 #3 vendor 的官方完整样例（134 KB）；
  2. 不改直接导出 → **逐字节相等**，且语义树与未知字段集合一致；
  3. 改单一字段 → `diffLines()` 的变更行集合**恰好等于**预期集合（与 v0.2.0
     `reference-index.test.ts` 同一手法，不是「包含」）；
  4. 再次导入导出 → 幂等；
  5. 六份专项样例各自走完整往返。
- **未知字段保留率 100% 是断言不是统计**：把上游样例里所有 P1/P2 协议字段的路径集合
  取出来，往返后逐条比对存在性与值。少一条即红。
- 专项样例**只新增不修改**，落在 `fixtures/` 下即自动受既有 `.prettierignore` /
  `.gitattributes` 规则保护，两个文件都不用改。
- 多行密钥样例复用 #10 的 WireGuard `private-key`；IPv6 样例覆盖 `bind-address` 与
  `nameserver` 两处。
- 若某份专项样例往返失败，那是 `yaml-engine` 的真实缺陷——**修引擎，不改样例迁就**。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表 FR-YAML-02 / 03 证据列补 golden 五步用例；
退出条件 3 满足。

**执行时决策与修正**：

- **先跑一次性验证脚本确认六份专项样例与 134KB 真实样例本来就能逐字节往返，
  再写正式断言**：`yaml-engine` 的往返保证是 M0/v0.1.0 就确立的基础能力，本片
  没有触发任何引擎缺陷——六份专项样例（含刻意选的边界写法：`common-health-check`
  这类不属于任何已建模字段的顶层锚点来源键、`"yes"`/`'null'` 这类需要引号才能保持
  字符串语义的值、`p@ss: w0rd! #not-a-comment` 这类引号内包含冒号/井号的密码）与
  真实上游样例全部一次性通过，不需要修引擎——计划原文准备好的"若失败修引擎不改样例"
  这条预案没有用上，如实记录而非假装真发生过一次修复。
- **未知字段（P1/P2）保留率的断言方式：逐路径逐值比对，不是数一下前后数量相不相等**：
  数量相等这种弱断言可能被"丢了一个又碰巧多了一个不相关的"这种情况骗过；本片按
  `collectUnknownFields()` 返回的每条 `PlannedField` 的路径建 `Map`，往返前后
  两个 `Map` 逐路径核对存在性与值——`collectUnknownFields` 是 v0.3.0 #16 交付、
  刚在 #17 修过跨模块共享根去重 bug 的同一套机制，这里是它在真实 134KB 文档规模上
  的又一次复验，不是重新发明一套统计口径。
- **单字段编辑测试选 `mixed-port`（真实样例第 3 行）**：该字段紧跟在两条被注释掉的
  同类字段（`port`/`socks-port`）后面且自身带行内中文注释，改动前特意确认了这一点
  ——选它是因为它同时能证明两件事：改动确实只触及预期那一行（`changedLineNumbers`
  精确到行号，不是"大致范围"），以及行内注释在被编辑的那一行本身也完整保留
  （新值后面那段中文注释原样还在）。
- **`golden.test.ts` 直接用 `@mcs/schema-builtin` 的六个真实模块常量，没有走
  `pipeline.test.ts` 里那套 `StoredBundle`/`createRegistry` 端到端路径**：后者要
  证明的是"签名校验 + 注册表解析"这条生产链路本身没问题，与本片"往返 + 未知字段
  保留"这个正交关注点无关，硬套上去除了多一层 `builtinAsStoredBundle()` 重复实现
  外没有额外收益。
- **专项样例的覆盖断言用 `listFixtures('yaml/golden')` 动态发现文件名，不是硬编码
  六个字符串**：与本仓库其余"覆盖性而非抽样"断言（`contrast.test.ts` 的 COLORS
  驱动、#18 `metadata.test.ts` 的字段动态收集）同一手法——以后再加第七份专项样例，
  这条测试自动跑到它，不需要记得同步改测试文件里的清单。

---

### #20 `packages/templates` 与两个内置模板

**目标**：交付「基础代理配置」与「仅代理集合 + 自动选择」两个内置模板。

**覆盖**：PRD §8.8（本版本 1–2 个）。

**依赖**：#10、#11。

**涉及文件**

- 新建：`packages/templates/package.json`、`tsconfig.json`、`src/index.ts`、`src/index.test.ts`
- 新建：`packages/templates/templates/{basic-proxy, provider-auto-select}/*.yaml` + 元数据
- 修改：根 `tsconfig.build.json`、`README.md`

**实现要点**

- 模板声明**适用的 Mihomo 版本范围与平台**（PRD §8.8 硬要求），版本范围引用 ADR-012
  的 v1.19.29，不写字面量。
- **「仅代理集合 + 自动选择」的 `proxy-groups` 段以未知字段形式原样写入**（E6）。
  `proxy-groups` 模块属 v0.4.0，本版本没有它的 Schema，因此该段在表单里编辑不了，
  只出现在 #16 的未知字段树与原文编辑器里。这不是妥协出来的缺陷，是本版本要证明的
  能力（PRD §8.3：未图形化的字段必须能通过原始 YAML 编辑并被无损保存）——要有一条
  用例断言该模板经完整往返后 `proxy-groups` 段**逐字节不变**。
- 因此本片有一条额外的交叉断言：模板产物导入后，未知字段告警是 `info` 级、
  `hasBlockingIssues() === false`（复用 #12 的口径），**不能因为有未知段就拦住导出**。
- **变量用明确占位符，绝不含真实密钥或订阅**。这条要有断言：扫描模板文本，
  命中疑似真实密钥形态（复用 `project-format` 的 `describeSensitivity` 判定）即红。
  这比人工 review 可靠。
- 模板产出的 YAML 必须先通过本仓库自己的校验流水线（#12 + #13）无 error——
  这是进内核测试之前的第一道闸门，本片就要断言。内核测试在 #21。
- 模板是数据文件，随 Bundle 发布（PRD §8.8 末条），因此布局与 `schema-builtin`
  保持一致，不发明第二套约定。
- 其余三个 MVP 模板（Android 生成目标、家庭路由器、规则集合分流）**明确不做**——
  版本文档说「只做 1–2 个」，且后两个依赖 v0.4.0 的 `rules` / `rule-providers` 模块。
  在追踪表里如实记为 Todo 并写明依赖。

**验收命令**

```bash
pnpm run check && pnpm run format:check
```

**完成后更新的文档行**：追踪表新增 §8.8 小节（两个模板 Done，其余三个 Todo + 依赖说明）；
README `templates/` 行改 ✅。

---

### #21 `tools/core-test-runner` 与 CI 内核测试 job

**目标**：建立「下载 v1.19.29 内核 → 校验 digest → 跑配置测试」的最小链路并接进 CI。

**覆盖**：PRD §13.3、§13.5 发布阻断项；退出条件 5。

**依赖**：#3（D-003 与 digest 值）、#20（有模板可测）。

**涉及文件**

- 新建：`tools/core-test-runner/package.json`、`tsconfig.json`、`src/index.ts`、
  `src/download.ts`、`src/download.test.ts`、`src/report.ts`、`src/report.test.ts`
- 修改：`.github/workflows/ci.yml`（新增 `core-config-test` job）、根 `tsconfig.build.json`

**实现要点**

- **放在 `tools/` 而不是 `packages/`**：它必须下载文件、写磁盘、执行子进程，
  这三件事在 `packages/**` 都被 ESLint / CI 禁止。`tools/**` 已被 ESLint 放开
  `no-restricted-imports`，且 `no-network-egress` job 只扫 `packages/`。
- **digest 表从第一天起就是多架构结构**（E2），不是单值常量：
  `Record<'linux-amd64' | 'linux-arm64' | 'darwin-arm64' | 'windows-amd64',
{ asset: string; sha256: string; bytes: number }>`，由 `process.platform` +
  `process.arch` 选行。本版本**只填 `linux-amd64` 一行**，其余留空并在缺行时明确报错
  （不静默回落到 amd64）。这样日后加 macOS / Windows runner 只是补数据，不动测试结构。
- **本版本已实测并可直接写入的一行**：`mihomo-linux-amd64-v1.19.29.gz`
  = `sha256:60de76a35a6cbf7b4fa4a20f5c257c24345d1d635ab1aa3877022a1997ef413c`，
  17 858 765 字节。注释必须写明「上游未发布 checksum 清单，此值取自 GitHub asset
  digest，见 D-003」——不写来源的话，后人无法判断这个值可不可信。
  **digest 不匹配就失败退出，不重试、不降级**。
- 单测覆盖多架构选行逻辑：命中行、未填行（必须明确报错）、digest 不匹配三条。
- **单测不下载**：把下载器拆成「取字节的注入端口」+「校验与解压的纯函数」，
  单测只测后者（含 digest 不匹配的否定用例）。真实下载只在 CI job 里发生。
- CI job 约束（ADR-010 §4 的同一姿态）：`permissions: contents: read`，不引入任何
  第三方 action，**不给该 job 任何 secret**。执行外部二进制的面已经扩大了，
  不能再叠加凭据。
- 配置测试用 `mihomo -t -f <模板产物>`，退出码非 0 即失败。job **阻断**（不是
  `continue-on-error`）——PRD §13.5 把「内置模板未通过内核测试」列为发布阻断项。
- **「仅代理集合 + 自动选择」模板带一个本版本未建 Schema 的 `proxy-groups` 段**（E6）。
  它必须照样通过内核测试——这是本版本对「未覆盖的段原样保留且内核仍可用」最有力的
  一条证据，比任何 golden 断言都直接。若内核对该段报错，说明模板本身写错了，
  修模板，不是把该段删掉绕过。
- 若 v1.19.29 的内核对某个模板报错而 PRD 认为该配置合法，按既有规则以上游为准，
  追加 D-xxx，**不得静默改模板去迎合**。

**验收命令**

```bash
pnpm run check && pnpm run format:check && node tools/core-test-runner/dist/index.js --dry-run
```

**完成后更新的文档行**：追踪表新增 PRD §13.3 行 → Done；README tools 区
`core-test-runner/` 由「规划中」改 ✅；退出条件 5 满足。

---

### #22 闭环验收、Alpha 离线产物与文档收口

**目标**：一次跑通 Alpha 的完整用户路径，验证构建产物离线可用，并把所有文档收敛到实测值。

**覆盖**：退出条件 1、4、7、8 的最终核对。

**依赖**：全部。

**涉及文件**

- 新建：`apps/web/src/alpha-loop.test.tsx`（v0.3.0 闭环用例）
- 修改：`docs/requirements-traceability.md`、`README.md`、`docs/releases/README.md`、
  本计划文件（进度表全部标 Done + SHA）

**实现要点**

- **闭环用例是一条**（沿用 v0.2.0 #17 的手法，`FakeWorker` 转发到真实
  `handleWorkerRequest`，不用预置响应）：新建项目 → 应用「基础代理配置」模板 →
  表单里改一个 `proxies` 字段 → 撤销 → 重做 → 切基础/高级模式 → 导出，
  断言导出内容与预期逐字节相等。
- **Alpha 离线产物验证**（退出条件 7）：`pnpm run build` 后，断开网络载入产物，
  确认可创建项目、渲染表单、导出。核对手段与记录写进
  `docs/releases/plans/v0.3.0-alpha-offline-evidence.md`；产物里**不得出现任何
  外部域名的运行时请求**（用 `read_network_requests` 核对并记录）。
- **退出条件 4 的机器核对**：一条用例遍历六个模块，断言每个模块的 `examples` 里
  `valid` / `invalid` / `edge` / `unknown-fields` 四类**各至少一份**（#2 已定义 `kind`）。
- 追踪表收口口径不变：**标 Done 必须能指向具体测试文件，否则记 Partial 并写明缺口**。
  已知会维持 Partial 的行要如实写清楚，不含糊：M0-5（真机推迟 v0.6.0）、
  FR-YAML-08、FR-RULE-_（v0.4.0）、FR-REL-04/06（v0.4.0）、FR-VAL-03（v0.4.0）、
  FR-UPD-02/05/06（v0.5.0）、FR-AND-_（v0.6.0）、NFR-SEC-07（部署配置）、
  NFR-A11Y（WCAG 2.2 全面走查）。
- 重跑 `pnpm run test:coverage` 取当时真值填表头，**不要抄本文件的数字**。

**验收命令**

```bash
pnpm run check && pnpm run format:check && pnpm run test:coverage && pnpm run build
```

**完成后更新的文档行**：追踪表头的日期 / 文件数 / 例数 / 覆盖率；里程碑映射表
「M2 及以后」拆出 M2 行并标已收口；README 项目状态改 M2 已完成、下一步 M3；
`docs/releases/README.md` v0.3.0 状态改「已完成」；本计划进度表全部 Done + SHA。

---

## 退出条件映射

版本文档的每条退出条件由哪个切片保证：

| #   | 退出条件                                             | 保证切片                                                   |
| --- | ---------------------------------------------------- | ---------------------------------------------------------- |
| 1   | FR-SCHEMA-01…06 全部 Done，每条指向具体测试文件      | 01→#14；02→#0+#1；03→#4；04→#18；05→#5；06→#10；总核对→#22 |
| 2   | 六个 P0 模块字段集合与 v1.19.29 核对完毕，差异已记录 | 基线与清单→#3；逐模块双向断言→#6、#7、#8、#9、#10、#11     |
| 3   | Golden 五步测试通过                                  | #19（样例来源 #3、#10）                                    |
| 4   | 每个 P0 模块有合法 / 非法 / 边界 / 未知字段样例      | `kind` 定义→#2；样例→#6–#11；齐全性机器核对→#22            |
| 5   | 内置模板通过 v1.19.29 内核配置测试                   | 模板→#20；内核链路与 CI job→#21                            |
| 6   | 基础 / 高级模式切换不丢字段，有自动化断言            | #14（导出文本逐字节断言）；联合分支侧→#0、#1               |
| 7   | Alpha 构建产物可离线运行                             | #22                                                        |
| 8   | `check` 与 `format:check` 全绿，覆盖率 ≥ 85%         | 每片的验收命令；收口核对→#22                               |

**无孤儿退出条件**：8 条全部有归属切片。

## 风险与待确认

### 风险

| 风险                                                   | 影响                                           | 缓解                                                                                                |
| ------------------------------------------------------ | ---------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 六个模块 + 九种协议的字段量超出 3 周（版本文档自列）   | 排期滑坡                                       | #3 先把 P0 边界机器化，P1/P2 字段一律标 `note: 'P1/P2'` 走未知字段树；#9/#10 已按协议复杂度拆成两片 |
| 134 KB 上游样例逐字段核对是本版本最大人工成本          | #3 拖长，阻塞 #6 起全部模块片                  | #3 无依赖，建议最先启动；产物是机器可读清单，一次核对换来 #6–#11 的自动断言                         |
| 判别式联合控件不足以表达九种协议的差异（版本文档自列） | `proxies` 做不出来，M2 失败                    | #0/#1 已提前解决控件类型；#9 一旦发现不足立即记 ADR-019 补充，不在渲染器里加协议 if                 |
| 受限 DSL 表达不了某些跨字段约束（版本文档自列）        | 被迫升级为应用代码（Level 2）                  | #4 明确要求「记录哪些约束被迫上升」，代价可见而非隐形                                               |
| **CI 下载并执行外部二进制**（E2 引入的新面）           | 供应链攻击面扩大                               | digest 写死并注明来源；job 不给任何 secret、不引第三方 action、`permissions` 最小化（#21）          |
| **内置 Bundle 签名在 #6–#11 每片都会失效**             | 忘了重签 → `verify.test.ts` 红或更糟：静默失配 | #6 交付哈希再签发回归断言，忘重签立刻红（E5）                                                       |
| 上游无 checksum 清单（本轮实测）                       | ADR-012「校验官方 checksum」无法字面执行       | 记 D-003，改用 GitHub asset digest；**不改 ADR-012 原文**                                           |
| `apps/web` 不在 `no-network-egress` 的 grep 范围内     | #16 的文档链接、#17 的订阅字段可能引入请求     | 两片各自用源码扫描用例断言无 `fetch`；#22 用真实网络面板核对产物                                    |

### 待你确认的问题

**无。** 计划编制期提出的三项已于 2026-08-18 全部答复并落进 E1 / E2 / E6：

| 问题              | 答复                                           | 落点                   |
| ----------------- | ---------------------------------------------- | ---------------------- |
| #3 vendor 落点    | 放 `packages/test-fixtures/fixtures/upstream/` | E1、#3 涉及文件        |
| #21 digest 表结构 | 多架构表，本版本只填 linux-amd64               | E2、#21 实现要点       |
| #20 第二个模板    | (a) `proxy-groups` 段以未知字段原样写入        | E6、#20 / #21 实现要点 |

`SOURCE.md` 的许可证措辞按计划写「引用上游示例配置作为比对基线」（#3 未另行指定）。

实现过程中若出现新的待决问题，按 v0.2.0 的先例写进对应切片的「执行时修正」，
不回填到本节。
