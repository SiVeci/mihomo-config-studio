# PRD 与上游实际行为的差异记录

规则：当 PRD 与 Mihomo 官方当前行为冲突时，**以官方行为为事实依据**，
但差异必须记录在此，不得静默修改需求。每条记录需注明核对日期与依据。

| #     | 主题                                           | PRD 表述                                                           | 实际情况                                                                                                                                                                                                                                                                                                                                                                                                                   | 处理                                                                                                                                                                                                                                                                                                       | 核对日期   |
| ----- | ---------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| D-001 | YAML 合并键 `<<`                               | PRD 未提及                                                         | Mihomo 使用 `gopkg.in/yaml.v3` 解析，支持 `<<` 合并键；`yaml` npm 包默认按 YAML 1.2 core 处理，`<<` 只是普通键                                                                                                                                                                                                                                                                                                             | `yaml-engine` 的 Composer 开启 `merge: true`，使领域投影与内核语义一致；文本层仍原样保留 `<<`                                                                                                                                                                                                              | 2026-08-10 |
| D-002 | 别名展开上限                                   | NFR-SEC-06 要求"别名扩展限制"                                      | `yaml@2.9.0` 的 `maxAliasCount` 是 **`toJS()` 选项**，不是 `Composer` 选项                                                                                                                                                                                                                                                                                                                                                 | 限制在 `toJS()` / `Node#toJS()` 调用点施加；解析阶段单独施加体积与深度限制                                                                                                                                                                                                                                 | 2026-08-10 |
| D-003 | Release checksum 清单                          | ADR-012 落点表要求"CI 固定下载 v1.19.29 二进制并校验官方 checksum" | v1.19.29 的 GitHub Release 没有发布任何 SHASUMS / `.sha256` 资产（本轮实测，`gh release view v1.19.29` 与 assets 列表逐项核对）                                                                                                                                                                                                                                                                                            | 改为固定 GitHub 报告的 asset digest（`sha256:60de76a35a6cbf7b4fa4a20f5c257c24345d1d635ab1aa3877022a1997ef413c`，对应 `mihomo-linux-amd64-v1.19.29.gz`，17 858 765 字节），digest 不匹配即失败退出、不重试不降级；值与来源写进 `tools/core-test-runner`（v0.3.0 #21）。**不改 ADR-012 原文**（已 Accepted） | 2026-08-18 |
| D-004 | `proxy-providers` 的 `filter`/`exclude-filter` | PRD §8.3「代理集合」P0 明确列出"过滤"                              | **已核实，PRD 表述准确，不构成真实差异**：vendor 的 `docs/config.yaml`（v1.19.29）样例本身没有直接演示（`filter` 只出现在 `proxy-groups` 的 `use:` 上，第 1711 行），但官方 Meta-Docs 源码（`github.com/MetaCubeX/Meta-Docs`，`docs/config/proxy-providers/index.md`）明确把 `filter`、`exclude-filter`、`exclude-type` 列为 `proxy-providers` 条目自身的顶层字段，并给出示例：`filter: "(?i)港\|hk\|hongkong\|hong kong"` | `packages/test-fixtures/src/upstream.ts` 的 `UPSTREAM_P0_FIELDS['proxy-providers']` 把 `filter`/`exclude-filter` 改记为 `presentUpstream: true`（含核实依据）；`exclude-type` 一并记录但不建模（不在计划文件 #11 的显式 P0 字段范围内）。#11（proxy-providers 模块）已按此结果实现                         | 2026-08-20 |

## 待核对项

以下条目需要在对应里程碑开始前对照官方文档与真实内核复核。核对基准版本为
**Mihomo v1.19.29**（[ADR-012](./adr/ADR-012-first-stable-compatibility-profile.md)）。

| 条目                                                          | 依据      | 截止        | 状态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------- | --------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 目标 Stable 兼容档案对应的 Mihomo 版本                        | PRD §19-2 | M0          | **已关闭**：v1.19.29，见 ADR-012                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| P0 出站协议字段集合与官方 `docs/config.yaml` 完整样例的一致性 | PRD §8.3  | M2 开工前   | **已关闭**：v1.19.29 的 `docs/config.yaml` 已 vendor 进 `packages/test-fixtures/fixtures/upstream/mihomo-v1.19.29/`，六个 P0 模块 + 九种出站协议逐字段核对，产物见 `packages/test-fixtures/src/upstream.ts` 的 `UPSTREAM_P0_FIELDS`；唯一未能从该样例确认的字段记入 D-004（v0.3.0 #3）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `rule-providers` 的 `format: mrs` 对 `behavior` 的取值约束    | PRD §8.3  | M3 开工前   | **已关闭**（v0.4.0 #0，2026-08-25）：双来源核对一致，`format: mrs` ⇒ `behavior ∈ {domain, ipcidr}`（`classical` 被明确排除）。来源一：仓库内已 vendor 的 v1.19.29 `docs/config.yaml` 第 1860-1870 行注释块。来源二：官方 Meta-Docs（`github.com/MetaCubeX/Meta-Docs`，`docs/config/rule-providers/index.md`，commit `89c2f10bc8bb1bbeb4898134db2931f2caa26f00`，经 `gh api` 拉取，2026-08-25 检索）原文「mrs目前 behavior 仅支持 domain/ipcidr」，与来源一逐字一致。不构成与 PRD §8.3「规则集合」P0 范围的真实差异（PRD 列的是各维度取值集合，未声称三×三全组合合法），故不新增 D-xxx。产物见 `packages/test-fixtures/src/upstream.ts` 的 `UPSTREAM_RULE_PROVIDER_CONSTRAINTS`；内核级实测留给 v0.4.0 #17 的模板内核测试矩阵（一条 `format: mrs` + `behavior: domain` 的 `rule-providers` 条目经 CI 的 `core-config-test` job 用真实 `mihomo -t` 验证） |
| Android 最低支持版本                                          | PRD §11.4 | M0-5 结束前 | **已关闭**：API 29（Android 10），模拟器口径，见 [ADR-014](./adr/ADR-014-android-minimum-supported-version.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

核对结果与 PRD 不一致时，按本文档开头的规则以官方行为为准，并在上表之前的差异表中
新增一条 D-xxx 记录，不得静默修改需求。
